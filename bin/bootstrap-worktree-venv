#!/usr/bin/env bash
set -euo pipefail

if [[ -e ".venv" ]]; then
  echo "An optional worktree-local .venv already exists at $(pwd)/.venv"
  exit 0
fi

python3 -m venv .venv
echo "Created optional worktree-local .venv at $(pwd)/.venv"
