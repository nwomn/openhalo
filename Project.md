# OpenHalo Project

## Current Snapshot

This is the canonical project baseline. Read this section first; load linked
detail documents only when the task needs their evidence or design depth.

| Field | Current baseline |
| --- | --- |
| Phase | Post-M16 multi-edge and productization expansion |
| Current milestone | `M17.11` / Issue `#19` Proxy Interaction Edge baseline |
| Parent milestone | `M17` multi-edge interaction expansion |
| Next route | `M17.11` -> `M17.10` -> `M20.1` -> `M21` -> `M22` -> `M23` -> `M24` |
| Parallel bounded validation | `M17.10` Camera Edge physical Feature validation is in progress; its full milestone remains later in the route and is not accepted |
| Deferred backlog | `M17.8` mobile sensitive-screen capture governance and `M17.9` native Windows Desktop Edge remain normal `Backlog` work for the current execution window; they do not block the active route and can be re-prioritized when implementation is scheduled |
| Immediate execution priority | Advance the visual Edge plus audio/microphone Edge perception-and-control closed loop using admitted base Observations, bounded evidence, ContextFact/ContextEnvelope, Main Hermes, Presence, validated action, and result verification. Generic Profile delivery, including the Camera Region Attention Overlay, is deliberately deferred; next M17.10 work continues with registered visual Features that do not require runtime configuration delivery. |
| Active parallel work allocation | This workstream owns `M17.11` Proxy Interaction Edge firmware development and real-hardware verification on the current Waveshare ESP32-P4-WIFI6 / C790 bench. Normal deployment requires a board-resident Proxy Edge service that starts automatically when the P4 powers on, retains its P-256 identity and provisioning locally, and reconnects without a general-purpose computer. ESP-KVM is only a replaceable capture/HID bring-up base; `codex/waveshare-p4-wifi6` is the owner's forked external ESP-KVM bench branch, not an OpenHalo source, release, or user-installation channel, and may hold only temporary board-port patches. The current Python `ProxyEdgeDaemon` is a development harness, not the deployment process. Before product delivery, the selected Proxy Edge firmware, hardware profile/manifest, signed artifacts, recovery path, and OTA workflow must be owned and released by OpenHalo, whether in this repository or a dedicated OpenHalo-owned firmware repository. After the OpenHalo Proxy Edge itself is accepted, selected drivers and recovery behavior may be retained in trimmed or replacement firmware. Compact PCB/package development proceeds separately. Firmware work may define and verify only the stable hardware-facing contract, and does not itself select, change, or accept the compact production PCB/package. This allocation does not use MaixCAM or `M17.10` Camera Edge evidence as the Proxy Edge hardware baseline. |
| Accepted baseline | `M17.0` through `M17.7.2`, `M18`, `M18.1`, `M4.1`, `M19`, `M20`, `M20.2`, and `M20.3` |
| Accepted cross-cutting slice | Hosted Coding Bridge adapter ([#14](https://github.com/nwomn/openhalo/issues/14)); parent/design tracked by [#10](https://github.com/nwomn/openhalo/issues/10) and [#11](https://github.com/nwomn/openhalo/issues/11) |
| Goal status | Goals 1-3 completed; Goal 4 runtime execution in progress; Goal 5 productization in progress; Goal 6 future research |
| Visual identity | Confirmed primary OpenHalo logo: the lower-left indigo `soft halo little ghost` with mint halo and muted periwinkle backdrop ([asset](assets/brand/openhalo-logo-primary.png)); selected 2026-08-23 |
| Execution source | [OpenHalo Development GitHub Project](https://github.com/users/nwomn/projects/1) |

Project tracking rules:

- Goals are the durable strategic baseline; GitHub Project `Initiative` is the active execution theme.
- Existing `M0` through `M24` identifiers remain historical identifiers.
- Future execution hierarchy uses Parent Issues and sub-issues rather than deeper milestone numbering.
- [`M17`](https://github.com/nwomn/openhalo/issues/2) is the parent Issue; [`M17.8`](https://github.com/nwomn/openhalo/issues/3), [`M17.9`](https://github.com/nwomn/openhalo/issues/4), [`M17.10`](https://github.com/nwomn/openhalo/issues/5), and [`M17.11`](https://github.com/nwomn/openhalo/issues/19) are its child Issues.
- `M24` Cellular Camera Edge is a separate later product-hardware milestone, not an `M17` child and not a prerequisite for accepting the MaixCAM-based `M17.10` validation slice. Its GitHub planning Issue is [#18](https://github.com/nwomn/openhalo/issues/18), assigned to the OpenHalo Development Project as `Backlog` / `Device Edge` / `Goal 5` / `P2`.
- A milestone becomes active only when its Parent Issue or child Issue is ready for implementation; later roadmap items remain planning drafts.

Progressive disclosure:

- Current implementation work: read the linked GitHub Issue and its plan/acceptance document.
- Architecture decisions: read the relevant file under `docs/plans/` or `docs/design/`.
- Completed evidence: read [`docs/history/project-completed-progress.md`](/root/openhalo/docs/history/project-completed-progress.md).
- M19 storage and owner-runtime evidence: read [`docs/history/m19-operational-status.md`](/root/openhalo/docs/history/m19-operational-status.md).
- Long-term research questions: read [`docs/research/project-open-questions.md`](/root/openhalo/docs/research/project-open-questions.md).

## Project Summary

OpenHalo is a new personal agent runtime system oriented around `device -> context -> presence -> action`, rather than the traditional `channel -> session -> agent` product shape.

The intended product is not "another chat agent entry point". OpenHalo is a personal runtime that can exist across multiple devices, maintain continuity across contexts, and decide how to surface itself through the most appropriate device or interaction surface. The current product direction is increasingly `presence-first`: the runtime should proactively infer user situation across input channels, decide whether to intervene, and learn intervention policy over time rather than waiting only for explicit user requests.

At the current stage, the project has moved from pure architecture-definition into an implemented and testable runtime baseline that now spans both the completed v0 single-edge WebSocket loop and the first same-template multi-edge routing slice. The architecture baseline and early milestone framing are in place, the first end-to-end desktop/CLI closed loop can be executed both in-process and across two real local processes, and the runtime can now route a normal action from one connected edge instance to another while preserving core state across restarts. The desktop/CLI surface has now been promoted into the first formal long-running terminal edge, with both user-initiated and runtime-initiated interaction still expressed through the normal `device -> context -> presence -> action` architecture rather than a chat-centered exception path. The current frontend baseline now includes both bounded scripted acceptance for repeatable verification and a true foreground live terminal session that reads user input from `stdin` on the same resident edge session.

The accepted M4.1 implementation moves colocated Host Edge process ownership into Personal Runtime: after Gateway readiness, Runtime starts one normal loopback Host Edge, persists redacted lifecycle diagnostics, retries failures with bounded exponential backoff and jitter, and cancels the session before Gateway shutdown. Its two-process human acceptance confirmed automatic Host Edge registration, a routed `runtime.status` action executed by `host-edge-1`, and clean shutdown without launching `host_daemon` separately.

## Naming Decision

- The project name is now `OpenHalo`.
- `OpenHalo` is the public/open-source project name for the presence-first personal runtime.
- `Personal Runtime` remains the backend architecture concept for the long-lived cross-device runtime core.
- Avoid expanding the name to `Halo OS` for now; keep `OpenHalo` as the project/repository-level name while the product shape remains open and exploratory.

## Background

The project started from dissatisfaction with OpenClaw's default architecture and product feel.

Current concern:

- OpenClaw feels like an agent system that the user must actively go to.
- Its default center of gravity is still closer to channels, sessions, and entry points.
- That shape does not naturally support a low-presence, multi-device, continuous personal assistant experience.

What we want instead:

- A multi-device personal assistant runtime
- Fast response paths, especially on local devices
- Low-presence behavior, meaning the assistant should be available without always being foregrounded
- Strong continuity across devices and contexts
- A system where `channel` is downgraded to a connector, rather than being the primary abstraction

## Current Working Architecture Direction

The project is currently aligned around a two-part system:

### 1. Frontend / Device Edge

Runs on user devices such as desktops, phones, earbuds, home nodes, and background monitors.

The first host-class edge may also run on the same cloud or server substrate that hosts the runtime itself, as long as it still participates through the normal edge boundary rather than becoming an implicit backend side channel.

Responsibilities:

- Local sensing
- Local interaction
- Local permission handling
- Local low-latency actions

### 2. Backend / Personal Runtime

Acts as the assistant runtime core rather than a traditional SaaS backend.

Responsibilities:

- Gateway / control-plane responsibilities
- Unified task and context state
- Agent-driven intervention, planning, and action generation
- Explicit presence-policy and intervention-governance inside the broader agent runtime
- External action orchestration

The current architecture baseline is documented in:

- `docs/plans/2026-06-16-runtime-architecture-design.md`
- `docs/plans/2026-06-18-goal2-presence-context-design.md`
- `docs/plans/2026-06-19-host-edge-v1-design.md`
- `docs/dev-env.md` for repository-level development environment workflow

Current boundary rules:

- `Frontend / Device Edge` is a device-resident edge runtime rather than a thin UI client
- `Backend / Personal Runtime` is a long-lived cross-device runtime rather than a traditional request-response backend
- `device`, `capability`, `context`, `agent`, and `action` are the primary runtime abstractions for this project
- `channel` and `session` are secondary implementation concepts and must not become the top-level product worldview
- `Presence` should remain an explicit, inspectable governance module inside the broader agent runtime rather than disappearing into opaque agent behavior
- All physical cross-boundary traffic must flow through `Edge Session Link <-> Gateway`
- Cross-boundary relationships between frontend and backend internal modules are logical only unless they pass through that transport choke point
- `Gateway` is a boundary and control-plane layer, not the primary reasoning layer
- `Presence` is a first-class governance layer inside the broader agent runtime, not just a device-routing helper; routing is only one sub-problem inside presence
- `Agent Runtime` is the primary intelligent runtime module; proposal formation and execution planning both belong inside that module rather than being modeled as separate top-level backend modules
- `Presence Router` should be treated as an explicit, inspectable governance submodule inside `Agent Runtime`, so intervention logic remains readable and more deterministic than a pure model-probability output
- 2026-08-24 `M24` formal Cellular Camera Edge product direction: the production fixed-camera Edge product line uses a MaixCAM-class low-cost local-vision base and adds self-contained cellular hardware, including a modem/baseband and suitable RF/antenna design; it does not require site Wi-Fi or Ethernet for ordinary operation. The later production form is an integrated PCB and purpose-designed enclosure derived from this class of platform, rather than resale of the MaixCAM development kit. The product line must offer both user-provisioned eUICC/eSIM and removable physical-SIM connection modes as equal v2 choices, but an individual SKU may be eSIM-only, physical-SIM-only, or dual-mode according to cost, carrier availability, and market needs. The user selects, activates, and pays the compatible carrier plan directly, while OpenHalo is neither a carrier nor a default traffic-billing intermediary. A phone companion may configure APN and, where the chosen modem/eSIM Local Profile Assistant and carrier permit it, relay an eSIM activation QR/code to the Edge over a local setup channel; no carrier lock is permitted. Cellular connectivity is only a transport bearer beneath the normal `Edge Session Link <-> Gateway`; it does not change P-256 device identity, Gateway control-plane validation, privacy governance, or Runtime semantics. The initial production traffic profile is low-bandwidth structured Observations, heartbeats, diagnostics, and explicitly authorized bounded evidence transfer; continuous raw-media uplink is not a default requirement. The exploratory price model excludes one-time integrated-PCB, enclosure, and hardware-R&D expenditure from per-unit sales cost; certification, manufacturing, warranty, and channel costs remain separate visible cost lines. Its provisional direct-sale price ladder is ¥299 physical-SIM, ¥349 eSIM, and ¥399 dual-mode, subject to later supplier quotes and a scale/feature review. Exact modem class, carrier/eSIM compatibility, regional certification, power budget, SKU matrix, and hardware platform remain later product-hardware design/procurement decisions. Its standalone plan is [M24 Cellular Camera Edge Design](docs/plans/2026-08-24-m24-cellular-camera-edge-design.md).
- The received Sipeed MaixCAM is explicitly the `M17.10` v1 scene, capability, privacy, and protocol-validation platform, not the `M24` production Camera Edge hardware reference. Its Wi-Fi-only deployment limitation must not be generalized into a product networking requirement; its verified P-256 and local-vision results remain valid evidence for the public Edge contract.
- 2026-08-05 / 2026-08-23 ambient camera Edge direction: the future fixed-camera `Device Edge` uses a Runtime-confirmed scene profile and allowlisted Feature subscriptions. The Edge performs local low-cost feature extraction, structured Observations, candidate-event detection, and bounded pre/post-event evidence buffering; `Personal Runtime` remains responsible for scene/profile governance, high-level video/audio understanding, evidence queries, Presence, and action decisions. Raw camera/microphone upload is not the default. The first physical validation target is a mains-powered fixed desk/room Edge using the received Sipeed MaixCAM standard kit (integrated display, Wi-Fi, and vendor camera), a user-provided 64 GB TF card that must first receive the MaixCAM system image, and a bounded read-only diagnostic status contract; it is not a wearable or custom PCB. Basic stock-device bring-up has passed (Wi-Fi, development-host connection, MaixPy `hello_maix.py`, and camera preview). The `riscv64` device lacks `cryptography`, a compiler, and Rust; current `cryptography 46.x` releases have no Linux `riscv64` wheel, so a narrow OpenSSL-backed P-256 Camera Edge bootstrap was implemented. On the real MaixCAM it created a persistent identity, produced an ECDSA-SHA256 proof accepted by the OpenHalo Gateway verifier, then completed one-time pairing and a subsequent no-code authenticated `edge.runtime.v2` reconnect with the owner Runtime at `ws://8.153.37.167:8765`; it now registers a structured `camera.health` capability and has completed one real accepted health Observation batch (`connected`, `not_checked`, `ready`, and free-space) with an atomically stored local status payload. The server retains an active, non-revoked `camera-edge-1` record. An attempted direct Maix display adapter was deliberately deferred: `Display()` starts the vendor multimedia/sensor stack and temporarily blocked device management, so it cannot yet be claimed as a status-only display or run beside the capture path. The first authorized local-vision Feature is now opt-in `person_presence.v1`: one Camera Edge process owns Maix camera + YOLO11, requires repeated matching local samples before changing state, and publishes only `{state: present|absent|unavailable, count, feature_version}` and confidence through `camera.person_presence`; raw frames, geometry, other labels, face/OCR/gesture inference, evidence buffering, and display are excluded. The initial physical run safely persisted `unavailable` during a sensor timeout; after an owner reboot, the repeated real-device run completed camera/NPU inference and Runtime persistence as `{state: absent, count: 0}` at `2026-08-23T09:48:17.464423Z`, with normal multimedia release. This is narrow Feature acceptance for `absent` and safe failure reporting only; it is not a `present`/identity evaluation, supervised service, Scene-Profile/Feature-Subscription governance, or full M17.10 acceptance. The repeatable owner-development procedure is in [MaixCAM Camera Edge Development Runbook](docs/ops/maixcam-camera-edge-runbook.md). Detailed design is recorded in [M17.10 Ambient Camera Edge Design](docs/plans/2026-08-05-m17-10-ambient-camera-edge-design.md).
- The working direction for proactive behavior is agent-centered but presence-governed: the runtime should allow the agent to form intervention proposals while requiring explicit presence decisions before user-facing intervention
- The runtime should support both a sense-first proactive path and an agent-initiative proactive path, and both paths must pass through `Presence Router` before user-facing intervention
- Edge-delivered signals may carry either passive environment evidence or explicit user-expressed intent; both still count as edge/context input on the sense-first path, and the runtime should interpret that input through normal proposal formation on the shared agent/presence path rather than treating any one edge type as a special case
- Proposal formation must distinguish passive observation evidence from user intent: not every observation should trigger interaction, but explicit requests, explicit commands, and approved user-configured intent triggers must receive a user-perceptible response or outcome through the presence-governed interaction model
- Ambiguous physical or environmental signals, such as an unexplained gesture captured by a future camera edge, should remain evidence by default; the same signal may become actionable intent only after user agreement, policy, or context explicitly promotes it into a configured trigger
- 2026-09-02 open semantic-cue direction: an unconfigured but apparently meaningful visual indication (such as waving, pointing, displaying an object, or a thumbs-up) is neither automatically ignored nor a direct command. A future registered `camera.semantic_cue_candidate.v1` Observation is nominated by cheap local change/person facts and immediately sends its deterministic source/interval/coverage/evidence-availability candidate envelope to Runtime; Edge cloud/local semantic understanding is explicitly not a prerequisite or a blocking first stage. Runtime admits it as ordinary ContextFact, and Presence decides silence, a low-interruption clarification on an appropriate surface, or supporting evidence in an already active interaction. A bounded asynchronous Edge worker may later attach a Markdown cue description to the same candidate/Episode, but cannot hold the Runtime acknowledgement/clarification hostage. Its semantic contract permits only “a clarification opportunity may exist”; it must not infer identity, addressee, consent, a command, or external-action authorization. Only an explicit reply, existing interaction confirmation, or a later owner-confirmed and revocable personal association can authorize action. This is future design work, not current gesture capability or M17.10 acceptance.
- Explicit cross-edge commands may require two semantic surfaces: the target edge performs the requested action, while the requesting edge receives acknowledgement, result reporting, or failure explanation; proposal formation should preserve both semantics instead of collapsing the whole turn into only the remote action surface
- The hot decision path should stay shallow and explicit: once an edge-driven event reaches deliberative handling, the preferred runtime-owned chain is `event -> compact snapshot -> grounding bundle -> prompt/context package -> model-backed proposal formation -> Presence Router -> execution planning/action`, and new intermediate representations on that path require a clear non-duplicative reason
- 2026-09-01 dual capability-contract decision: contracts belong to an Edge's registered Action or Observation, never to Personal Runtime as an implicit provider. Every rich Action registration must carry a versioned `machine_contract` (schema, side effect, result states, confirmation boundary) and a separately projected `semantic_contract` (purpose, success meaning, limitations); every Observation registration must carry its matching mechanical schema/freshness contract plus a Main-Hermes semantic contract (meaning, permitted inference, must-not-infer). Runtime only validates and projects the registered contracts; ContextFact/grounding carries the bounded semantic half, and neither semantic prose nor an Observation can bypass Presence, Execution Planning, or Action Layer. The Runtime legacy string registry remains routing metadata only and must not manufacture a dual contract for an Edge. Missing, legacy, or contradictory Action/Observation contracts fail closed at registration, so an unupgraded Edge cannot enter the Runtime as a partially described provider.
- 2026-08-30 interoceptive Harness direction: future persistent Main Hermes / Runtime Harness engineering must continuously maintain and, on every deliberative proposal-formation turn, consult a bounded Runtime-owned interoceptive state. This state is distinct from Edge-derived external observations and represents the agent/runtime's own operating condition, such as health and recovery state, resource/latency/evidence budget, capability and connectivity fidelity, recent action outcomes, and calibrated uncertainty. It is compiled into the existing grounding bundle or prompt/context package rather than becoming a new top-level module or a duplicate hot-path context carrier. Its role is to help proposal formation adapt priorities, caution, evidence requests, recovery, or deferral to the Runtime's current condition; it cannot directly produce user-facing action, bypass explicit user intent, permissions, deterministic validation, or the final `Presence Router` governance decision. This is future Harness work and does not alter the active M17 route or accept any current milestone.
- 2026-08-30 Edge Attention Profile baseline: a future Edge Profile is not a second lossy Observation gate. Safe bounded structured base Observations remain admissible independently of a Profile, enter ContextFact with provenance/freshness/privacy, and are selected for Main Hermes by ContextEnvelope rather than being all injected into a prompt. Main Hermes may later propose a short-lived target/surface-specific Attention Profile for additional registered Features, cadence, local evidence retention, or evidence-query triggers; Runtime validates consent, capability, privacy, budget, and binding, while a delivery component only sends/renews/retries an accepted overlay. The current Proxy `proxy.screen.profile.configure` hard activation protocol remains a host/runtime experiment; Profile delivery implementation is explicitly deferred and must not block the immediate visual plus audio/microphone Edge perception-and-control closed loop. Detailed baseline: [Edge Attention Profile Baseline](docs/design/2026-08-30-edge-attention-profile-baseline.md).
- 2026-08-30 camera/audio design alignment: the M17.10 Camera Edge design no longer uses a Scene Profile or Feature Subscription as a gate for base Observation admission. Its camera and audio/microphone features follow the same base-Observation plus future Attention Profile overlay baseline; bounded evidence, consent, privacy, retention, and Runtime Understanding constraints remain unchanged.
- 2026-08-30 M17.11 Proxy screen history and retained-evidence baseline: the P4 Proxy Edge owns a RAM/PSRAM-only five-minute live screen ring (64 slots, 96 KiB per frame, about 6 MiB total, 300-second TTL). Every frame receives a device/boot-scoped `evidence_id`; ordinary `proxy.screen.read` returns the latest frame and ID, while a later `cached` read must name that exact live ID. The generic `action_request`/`action_result` envelope remains unchanged; eviction, TTL expiry, reboot, foreign IDs, and SHA mismatch fail closed without substituting another frame. A successful bounded JPEG action result may be retained as a bounded encrypted local Runtime evidence object with exact provenance (`evidence_id`, timestamp, SHA-256, source device and surface). `InteractionPool` remains modality-agnostic: a normal Interaction may reference that evidence alongside any other facts, use its ordinary child session, ordered mailbox, retry policy, question/answer history and bounded SemanticDeltas, and return ordinary conclusions and uncertainty to Main Hermes. The Orchestrator's decision to create or continue an Interaction in order to explore an image is only one use of this general Interaction/evidence mechanism; it creates no image-specific lifecycle, routing, child-session type, or Main-Hermes shortcut. Main Hermes receives conclusions and evidence references rather than raw image bytes, and may use the same generic continuation mechanism to request another bounded examination of retained evidence. Retention, encryption, access audit, explicit deletion, byte/count limits, and TTL are Runtime evidence-policy concerns; provider-hosted conversation retention is not used as the evidence store. The current P4 and Python reference implementations carry the Edge ring and generic attachment contract, but Runtime still strips JPEG bytes and only retains metadata/Understanding, so durable retained-evidence storage, generic evidence-to-Interaction orchestration, retry telemetry, and real latest/cached public-Gateway acceptance remain implementation work.
- 2026-08-30 Camera Edge v1 development focus: the MaixCAM validation prioritized making the present scene legible to Main Hermes, rather than validating only person presence. The first capability family is visual quality/availability, person count and transitions, region occupancy, allowlisted object presence, OCR, face/gesture/pose signals, and one bounded `camera.visual_foreground.v1` summary of the one-to-three locally most salient structured facts. Camera Edge must not turn that summary into free-form local reasoning: it carries exact typed feature results, confidence, freshness, and a local salience reason for Main Hermes to interpret. Its built-in microphone is also in scope for development validation through separate `audio.speech_activity.v1` and `audio.addressing.v1` Observations. VAD alone never proves that speech is addressed to OpenHalo. The first positive addressing contract deliberately uses an explicit configured wake word, button/gesture, or bounded recognized address phrase; `ambiguous`, ordinary conversation, and unavailable audio remain non-interrupting passive context by default. A later Runtime/Hermes and Presence capability may use the ongoing structured conversational scene to propose whether to supplement a discussion or remain silent, but it must be evidence-backed, inspectable, and cannot bypass Presence governance. During owner-controlled development validation, bounded local raw camera/audio may be temporarily inspected to establish feature ground truth, but they are not ordinary Runtime context or default uploads. This expanded the M17.10 v1 validation surface; it neither accepts M17.10 nor replaces the later independently accepted standalone Audio/Microphone Edge and Multimodal Edge work.
- 2026-08-30 Camera Edge visual capability implementation: `MaixPersonPresenceFeature` now owns a single shared MaixCAM + YOLO11 sampling pass. It preserves the existing `camera.person_presence.v1` wire contract and registers separate `camera.object_presence.v1`, `camera.region_occupancy.v1`, `camera.scene_quality.v1`, debounced `camera.person_presence_transition.v1`, and `camera.region_occupancy_transition.v1` providers when the visual Feature is active. Object labels are explicit allowlists; regions are normalized person-occupancy rectangles; scene quality is currently limited to capture availability and detector dimensions until a verified image-quality API is added. The host regression slice covers shared single-pass semantics, unavailable-versus-absent behavior, bounded frame shape, registration, and person/region transition mapping. That host-only implementation did not by itself accept the widened capability set or M17.10. An owner-controlled SSH-only rerun on 2026-08-30 found the MaixPy `NN`/YOLO constructors aborting with native `SIGSEGV`; an abnormal exit was followed by a capture-probe `No buffer space available` failure, while health-only reconnect remained healthy. After the owner re-flashed the MaixCAM system image, the new image accepted SSH re-authorization and the bounded physical rerun passed: camera single-frame read, YOLO11 initialization and one detection (`5` detections), health Observation, and visual Observation persistence for `camera.person_presence.v1` (`absent`, count `0`), `camera.object_presence.v1` (`person: 0`), `camera.region_occupancy.v1` (`center` unoccupied), and `camera.scene_quality.v1` (`ready`, `320x224`). A follow-up health capture probe also persisted `camera.capture_state=ready` at `2026-08-30T13:15:35Z`. The Runtime SQLite integrity check was `ok`, and a no-code P-256 reconnect succeeded at `2026-08-30T13:14:27Z`. This accepts the post-reflash narrow local-vision and Runtime-persistence slice only; positive person/object/region cases, long-running supervision/recovery, OCR/face/gesture/pose/audio, and full M17.10 acceptance remain open.
- 2026-08-31 Camera Edge person-presence acceptance: the owner repeated the real MaixCAM in/out test while a read-only query inspected the owner Runtime SQLite history. `camera-edge-1` persisted `camera.person_presence.v1` as `present` (`count=1`), then `absent` (`count=0`) and `camera.person_presence_transition.v1` `present -> absent` / `left` at `2026-08-31T09:28:59.404228Z`; after the owner returned, it persisted `absent -> present` / `entered` and `count=1` at `2026-08-31T09:29:50.612187Z`. This accepts the bounded real-device person-presence and debounced entered/left transition contract across `MaixCAM Device Edge -> Edge Session Link -> Gateway -> Personal Runtime`. It does not accept an empty/default region as a configured-region scenario, and it does not accept identity, audio, Evidence/raw-media, boot/recovery supervision, or full M17.10.
- 2026-08-31 Camera Region Attention Overlay decision: static regions in the Camera App's private configuration are diagnostic fallback only and are not a useful product control surface. A future Runtime-owned hot-applied overlay may let Main Hermes propose a bounded named region for one paired Camera Edge, while Runtime validates owner consent/policy, the registered capability, target binding, normalized bounds, privacy/resource budget, revision, and expiry before dispatching a registered configuration action. Edge would atomically apply or reject the declarative in-memory overlay without an App restart, return a correlated result, and tag region facts/transitions with the accepted revision; expiry, replacement, and revocation would remove it safely. The payload excludes arbitrary detector code, free-form prompts, raw-media authorization, identity templates, and action policy. Region facts remain passive context and cannot bypass Presence. This is a deferred design, not the next M17.10 delivery.
- 2026-08-30 person-identity memory plan: generic person detection never implies that the observed person is the owner. OpenHalo needs Runtime-owned abstract `Person` entities that associate owner-approved reference photos or local face templates, names/aliases and relationships, and bounded recognition-confirmation history. Camera Edge remains a source of local face tracks and typed identity-candidate evidence; it must emit an explicit confidence-bounded result such as `owner`, a named known person, `unknown`, or `ambiguous`, never silently label every detected person as the owner. Main Hermes consumes person entities and evidence-backed scene roles rather than raw camera geometry, while Presence may use the distinction between owner, known visitor, and unknown person only through explicit policy. During owner-controlled development, local photos may establish enrollment and ground truth; raw face media is not ordinary Runtime context or default transport. The M17.10 implementation plan now explicitly sequences the Person registry, local face-template cache, `camera.person_identity_candidate.v1`, temporal/multi-person uncertainty handling, enrollment correction/revocation, and phone-side multiple-person acceptance. This is a concrete plan, not yet a face-recognition implementation or acceptance result.
- 2026-09-01 layered evidence-memory direction: this is a future end-to-end `State / Context` capability of Personal Runtime, not a Camera-only pipeline, a new top-level Runtime lifecycle, or current M17.10 acceptance. Any paired Edge surface may participate through one generic source-scoped evidence contract: Camera video/audio, Proxy screen history, and future media sources each retain their own owner-enabled capture, visible indicator where applicable, local `HotRing`/ephemeral evidence handles, segment lifecycle, capacity health, and source-specific privacy limits. Runtime owns the common per-source retention/privacy/budget policy, explicit evidence-upload authorization, durable structured `Episode` / timeline index and retrieval, model/provider provenance, coverage/uncertainty/limitation records, `pinned_evidence`, deletion audit, and user-query routing; its records must name `edge_id`, capability/surface, source timestamp and clock uncertainty, and never collapse concurrent sources into one anonymous camera. Source Episodes remain independently retrievable; only a query-time correlator may create a provenance-preserving composite Episode from aligned observations/evidence across multiple sources. Base typed Features/transitions remain the immediate low-latency ContextFact path; raw media never enters ordinary ContextEnvelope or Presence directly. A long-term Episode is not an assertion that every detail was observed: claims distinguish source-observed facts from model inference and preserve missing coverage. Uploading every sealed five-minute raw chunk would be delayed continuous raw-media upload and is therefore not the default: ordinary archival compiles low-cost source-local Feature timelines with configurable sparse visual sampling; a candidate event, user request, or approved policy may escalate a bounded clip to a stronger video model. Detailed queries first use the named source's HotRing; after raw expiry, Runtime must state that an Episode cannot reliably verify unavailable fine detail. Required future acceptance includes source isolation and multi-Edge correlation, explicit video/audio/screen opt-in and indicators, pause/clear/delete, bounded storage/backpressure and failure states, no hot-path blocking, authorized upload, Episode provenance/retrieval, and deletion/pinning behavior.
- 2026-09-01 Main Hermes evidence-memory context decision: Main Hermes does not receive raw media, an unbounded timeline, or all durable Episodes as implicit memory. The existing Runtime-owned ContextEnvelope compiler produces a bounded query- and interaction-relevant `MemoryContext` alongside current ContextFacts: one time anchor; selected source or composite Episode claims with stable IDs, interval, source/provenance, observed-versus-inferred classification, confidence, coverage and limitations; explicit conflicts/unknowns; and only references to policy-eligible HotRing or retained evidence. Deterministic Runtime retrieval first resolves time/source/intent and byte/count budgets, then compiles the candidate set before Main Hermes reasons; Hermes may use the normal bounded evidence-query/action path to request a specific finer clip or additional summary, after which Runtime validates authorization and returns a new bounded Understanding for the next semantic turn. The Main response must preserve uncertainty and never promote an Episode inference, sparse sample, expired raw media, or unauthorised identity claim into fact. Historical media memories become proactive inputs only through separately configured policy and ordinary Presence governance; a user-initiated recall is a normal Interaction but does not bypass the same privacy/evidence policy. This extends ContextFact/ContextEnvelope and the existing generic evidence mechanism without a second model-memory store, Camera-specific prompt, or Main-Hermes shortcut.
- 2026-09-01 distributed media-memory v1 simplification: the first working loop deliberately permits an owner-enabled Edge to retain the provider's primary API key privately and send its own sealed media directly to the selected provider; Runtime need not issue an inference lease or perform per-upload audit before this workflow is proven. This does not authorize continuous raw-media upload by default: the owner must enable/configure the source, sampling, bitrate, and retention explicitly. The cloud model's required semantic output is a bounded Markdown `EpisodeNote` for Main Hermes, accompanied only by a deterministic source/timing/status envelope and local typed Feature timeline; strict model-produced JSON is not a v1 correctness dependency. An offline or repeatedly failing source retains sealed raw media only inside its configured local archive ring; on rotation it drops the oldest unsummarized media and reports the coverage gap, while protecting the independently configured Hot Ring. Scoped inference credentials, detailed upload audit, adaptive gating, and optional derived structured claims remain later hardening rather than v1 blockers.
- 2026-09-01 short-term media query routing: recent detailed questions also do not route media through Runtime. Runtime sends a registered `media.memory.query` action containing `source_ref`, time window, and question to the corresponding Edge; the Edge extracts a bounded local Hot Ring interval, calls its configured model directly, and returns textual `Understanding` with small source/coverage/limitation metadata. Runtime handles only the action, text result, and metadata, then owns multi-source orchestration, context injection, and the final user-facing expression; raw video/audio never enters or persists in Runtime.
- 2026-09-01 media-memory implementation slice: a shared Edge-local persistent segmented `LocalHotRing` and registered `media.memory.query` executor now exist. The executor accepts a source-bound, bounded time-window question, gives only selected local segments to an injected Edge-side understanding provider, and returns Markdown `Understanding` plus model/coverage/limitations metadata as a normal `action_result`; it has no media-byte result field. `CameraHealthDaemon` advertises and handles that action only when a local media executor is configured. Focused regression verifies Hot Ring capacity rotation, unavailable-interval handling, Camera registration/dispatch, and Runtime persistence of text without local media bytes. This is a contract/test slice, not MaixCAM continuous encoding, a concrete provider integration, Episode generation/sync/indexing, or M17.10 acceptance.
- 2026-09-01 CameraEdgeService single-process topology: the current implementation direction is one Camera Edge process and one asyncio event loop, with `CameraEdgeService` as the only camera owner. Each capture iteration fans its ephemeral frame out locally to `SegmentRecorder -> LocalHotRing` and a sampled `FeatureWorker ->` small Observation sink; a separate bounded in-process action queue waits for direct-provider `media.memory.query` work and emits only textual action results. Slow model requests therefore do not block later capture iterations, and neither raw frames nor encoded segment bytes enter a service queue or the Gateway. `CameraHealthDaemon` remains a compatibility Gateway/session and health entry point during migration and must not become another camera owner. The future two-process control-agent/data-plane topology remains an optional later hardening, not this slice. Focused regressions cover same-frame local fan-out, local Hot Ring admission, Feature emission, asynchronous provider wait while capture progresses, and media-free result output. This is still not real MaixCAM encoder/provider/long-term-memory acceptance.
- 2026-09-01 Runtime-selected provider-profile action slice: `media.provider.configure` is not a user-supplied bare-key action. Runtime resolves one of its own configured `ProviderConfig + ModelConfig` entries by `model_ref` and projects the complete callable profile to the paired Camera Edge: provider name, adapter type, base URL, wire API, timeout, default headers, API key, and selected model/capability identity. The Edge retains that full profile only in process memory; its result exposes only non-secret provider/model identity and `configured`, and restart deliberately requires re-provisioning. `CameraEdgeService` routes this action through its bounded local worker; `CameraHealthDaemon` may advertise/dispatch it when supplied the same credential store. This is an explicit v1 functionality choice, not durable secret storage, scoped credentials, secret audit, automatic video-capability selection, or a real video-provider integration.
- 2026-09-01 direct-Edge provider eligibility correction: an ordinary Runtime chat profile must not be assumed safe or capable for Camera Edge media traffic. Runtime now requires an explicit `edge_direct_eligible = true` provider setting before building `media.provider.configure`; the default is reject. A minimal real probe of the deployed `cubence / openai_compatible / gpt-5.6-terra` profile returned an explicit provider anti-secondary-distribution rejection, while official GPT-5.5/5.6 documentation states video input is unsupported. That deployed profile is therefore ineligible for Camera Edge and must not be dispatched, despite its Runtime `supports_vision` flag. Choosing a separately authorized direct-Edge provider with the needed video (or deliberately implementing a sampled-image fallback) remains a required next configuration decision.
- 2026-09-01 Camera Edge DashScope video-provider slice: the deployed private Runtime configuration now has one fixed mapping, `camera-edge-1 -> camera_video_qwen3_vl_flash -> camera_video_dashscope / qwen3-vl-flash`, with `openai_compatible`, `chat_completions`, `supports_video = true`, and explicit direct-Edge eligibility. A minimal real 16x16 image request to the configured Beijing endpoint returned `VISION_OK`, confirming endpoint, model, and credential availability without exposing the key. The new Edge-local `OpenAICompatibleVideoAdapter` turns selected local MP4 Hot Ring segments into OpenAI-compatible `video_url` Base64 inputs and returns Markdown Understanding; it limits each raw inline segment to 7 MiB (provider inline limit boundary) and never sends media to Runtime. Runtime configuration now also carries an explicit `llm.edge_media.<device_id>.model_ref` mapping, rather than dynamically scoring profiles. After a Camera authenticates and announces `media.provider.configure`, Runtime automatically dispatches its fixed mapped profile and Edge must re-receive it after restart; the action result remains non-secret. The synchronized `~/openhalo` server test tree passed 58 focused Runtime checks and built the real private-config action for `camera-edge-1` with a present credential, without disclosing it. The currently online Runtime is an older, separate release tree and was not restarted or changed by that test synchronization. This does not accept a real video request yet: true MaixCAM encoding/segment capture remains unfinished.
- 2026-09-01 Camera Edge single-owner capture implementation: `CameraEdgeService` is now the App's concrete local data-plane loop rather than only a test seam. `MaixCameraCaptureOwner` is the sole `maix.camera.Camera` owner and supplies one NV21 frame to a single H.264 MP4 segment encoder and the refactored YOLO Feature worker; the latter consumes an injected frame and no longer opens a second camera in this path. `CameraHealthDaemon` keeps the Gateway session/health role but, when given this service, sends the service's small observation frames and asynchronous text-only media action results over the authenticated socket. The Maix App package manifest includes the new modules, and the owner-visible config has `media_memory_enabled = false` by default with bounded Hot Ring/segment parameters for explicit opt-in. Focused desktop contract tests pass for the App construction, shared-frame Feature inference, single-service capture/action behavior, and provider adapter. This is not yet real-device acceptance: Maix hardware H.264/MP4 sealing, provider video request, action delivery from the online release, storage pressure, and long-duration supervision remain required.
- 2026-09-01 Camera Edge media-memory release: commit `6894223cc655579d4186016666f18ea9b584ef1e` was published as immutable GitHub Release `v0.1.30`; the owner server safely stashed its prior test-tree changes, fast-forwarded `~/openhalo` to that commit, and `openhalo update` atomically selected the corresponding installed release. Its release gate passed 95 focused Camera/Runtime/guard regressions. The broad Windows suite remains non-gating because it fails before Camera tests on an existing Linux-only `.venv/bin/python` subprocess path. The initial real-device run exposed a MaixPy API mismatch: `maix.video.Encoder` has no `finish()` and does not finalize an MP4 file itself.
- 2026-09-01 Camera Edge local Hot Ring physical acceptance: the USB-attached MaixCAM is directly reachable through its NCM/RNDIS interfaces (`10.89.97.1` / `10.89.98.1`), so USB network is the preferred developer control/debug path; its Wi-Fi address is only an alternative transport. The `v0.1.31` recorder repair writes the encoder's returned H.264 frames to a temporary elementary stream, releases the one native encoder, then uses the board-local `ffmpeg` to seal MP4 before local Hot Ring admission. On the physical device, the long-running single-owner service produced seven 1.1–2.1 second `video/mp4` Hot Ring segments, and `ffprobe` confirmed each as a readable MP4 without service errors. This accepts only local capture -> encode -> seal -> bounded Hot Ring behavior and the shared local capture loop; provider configuration receipt, a real provider video Understanding, action-result return, retention-pressure, long-duration supervision, Episode/archive memory, and M17.10 remain unaccepted.
- 2026-09-02 Camera Edge physical regression investigation: the newer simultaneous YOLO + recorder profile at `640×480/10fps` exhausts the Maix multimedia pools (`VI_SDK_IOC_S_CTRL ... Out of memory` / `IOCTL_VB_CMD ... create_pool NG`), and a failed YOLO frame then repeatedly reloaded the native model once per feature interval. The service now retains the detector after a failed inference and defaults new App configurations to the lower `320×224/5fps` model-aligned profile; focused Camera regressions pass. The device reconnected and reported `connected` after this change, but the native H.264 encoder still did not emit bytes in the simultaneous profile, while standalone encoder probing also hit `No buffer space available`/SIGSEGV. Consequently the earlier narrow local-Hot-Ring acceptance is not generalized to the full recorder-plus-YOLO configuration, and real provider/action closed-loop acceptance remains blocked pending an empirically stable Maix multimedia capture/encoder profile.
- 2026-09-02 Maix native-recorder migration slice: the manual `Camera.read() -> Encoder.encode(frame)` path has been replaced in the Maix App by a `VideoRecorder`-bound capture owner. It binds the sole Camera before any vendor recorder configuration (a MaixPy ordering requirement), keeps a supported 640×480 recording profile separate from a 320×224 RGB `snapshot()` Feature input, and seals each native MP4 into the local Hot Ring without Runtime media transfer. Twelve focused Camera regressions pass. On the rebooted physical MaixCAM, this process remained online and Runtime-connected but blocked during native Camera/VideoRecorder initialization before an active MP4 or snapshot was emitted; no Python exception occurred. This is implementation progress, not physical acceptance: the stable vendor capture/recorder initialization sequence remains to be demonstrated before Hot Ring or provider-loop acceptance can resume.
- 2026-09-02 bound-Encoder decision: `VideoRecorder` is no longer the Camera Edge recording path. The Maix App now follows the vendor hardware-binding topology: one Camera is bound to one `video.Encoder(capture=True)`; each loop calls `encode()` for H.264 and `capture()` for the local YOLO image. The H.264 stream is independently sealed to MP4 and admitted to Hot Ring. The updated focused Camera suite passes 13 tests. A first on-device launch after stopping the previously blocked VideoRecorder process still failed during Camera initialization with the existing driver `Out of memory`/SIGSEGV signature, before any new bound-Encoder code ran. A rebooted-first-launch physical check remains required; this does not accept the new encoder path, Hot Ring, or provider loop.
- 2026-09-02 bound-Encoder physical progress: after reboot, the bound-Encoder App was the first Camera process and remained Runtime-connected while continuously producing a multi-megabyte H.264 elementary stream; this physically validates the single Camera -> `Encoder.bind_camera` -> `encode()` and `capture()` topology. The first MP4 seal is not accepted: the independent segment begins on a P-frame and lacks H.264 SPS/PPS parameter sets, so board-local `ffmpeg` cannot parse the slice stream and Hot Ring stays empty. The next bounded recorder repair is to retain/re-admit the stream parameter sets and start each seal on an independently decodable keyframe; it must also keep packaging off the capture hot path. Provider Understanding/action-return acceptance remains blocked on that MP4 result.
- 2026-09-02 H.264 keyframe-segmentation acceptance: the bound-Encoder segmenter now caches Annex-B SPS/PPS NAL units, begins a raw candidate only on an IDR, and closes it only at a later IDR after the requested duration; every candidate is prepended with its parameters before MP4 packaging. Closed raw chunks are passed to a separate local packaging worker, so ffmpeg no longer stalls capture. On the physical MaixCAM, fresh Hot Ring `.media` artifacts now probe as readable `mov,mp4` H.264 `640×480` video (a verified new segment was 158,067 bytes). This accepts Camera -> bound Encoder -> IDR-safe H.264 -> asynchronous MP4 seal -> local bounded Hot Ring. The restart test also exposed the known stale Runtime WebSocket ownership after a duplicate Camera launch; that is a control-plane reconnect issue, not a media-path rejection. Real provider Understanding/action-return and ordinary-chat acceptance remain separate pending work.
- 2026-09-02 Gateway authenticated-session takeover: a reconnect for an already-paired device is no longer rejected merely because a former WebSocket has not yet observed its close. Only after the new socket completes the existing P-256 authentication proof does Gateway atomically replace the live connection and close the former socket with an explicit superseded-session close. The former handler's identity-safe cleanup cannot remove the replacement. A different pairing identity remains unable to take over or consume a new pairing code. Focused WebSocket tests cover verified takeover, superseded close, and untrusted duplicate rejection. This removes a Runtime-restart workaround for Camera Edge reconnect; it does not accept the remaining media-provider/chat loop.
- 2026-09-02 authenticated-session takeover deployment: the focused Linux Gateway regression passed 4/4 for verified same-identity takeover, superseded-session close, identity-safe old-session cleanup, and untrusted duplicate rejection. The change was published as `v0.1.37` (`6bbd060`), and the owner Runtime was updated and restarted on that release. The USB-attached Camera Edge automatically reconnected, resumed visual Observations, and received its registration-time media-provider configuration. This accepts the stale-WebSocket reconnect repair; real video Understanding and ordinary-chat completion remain separate acceptance work.
- 2026-09-02 Camera Edge chat-routing validation: the USB-attached service was refreshed with the current media contract and restarted; a temporary paired Terminal Edge sent a normal Chinese scene question through the live Runtime. Main Hermes selected `media.memory.query` and Runtime dispatched it to `camera-edge-1`, proving the generic chat-to-action path. The first request returned an explicit `media_source_mismatch` because the model was not shown the canonical source identifier; the registration projection now exposes `source_ref` and Hermes is instructed to copy it exactly. A successful provider video Understanding still requires one follow-up live query after this contract fix.
- 2026-09-02 Camera media boundary hardening: Edge now rejects reserved caller-supplied headers that could override Authorization/Host framing, caps the aggregate inline video payload and provider response size, and preserves a diagnostic action result when the media worker raises unexpectedly. Focused media and Camera Edge regressions pass; these limits also address the likely low-memory failure mode observed during the first real multi-segment query.
- 2026-09-02 Camera media failure recovery slice: every IDR-safe H.264 seal now writes bounded JSONL diagnostics with SPS/PPS cache state, opening-IDR and IDR-count state, seal times, and the bounded `ffmpeg` stderr/result. A packaging failure immediately marks the Camera media pipeline unavailable, makes `media.memory.query` return an explicit correlated `media_pipeline_unavailable` result instead of querying stale Hot Ring content, then closes the recorder and bound encoder so the next capture loop reinitializes a fresh stream. A later successfully sealed MP4 restores availability. Focused recorder/service regressions pass; this is diagnosability and recovery behavior, not a physical long-duration or provider-loop acceptance.
- 2026-09-02 Camera media diagnostics physical check: the recovery build was synchronized over the USB NCM connection and launched as one confirmed Camera Edge process. Its live diagnostic JSONL recorded successive independently sealed segments with cached SPS/PPS, one opening IDR per segment, exact time boundaries, and successful bounded `ffmpeg` results; matching MP4 Hot Ring artifacts continued to appear. This confirms the added observability path on the device, but not the failure/restart branch under real hardware or the outstanding provider/chat closed loop.
- 2026-09-02 Maix App lifecycle correction: direct `/root` debug-copy launches are not a valid Camera Edge deployment or Launcher-health signal. The temporary process was stopped; the current Camera Edge sources were repackaged as Maix App `openhalo_camera_edge` `0.1.4` and installed through the device `app_store_cli` into `/maixapp/apps/openhalo_camera_edge`. The next physical step is a single owner Launcher start of that installed App, followed by process/log/Hot-Ring observation; installation alone is not launch or closed-loop acceptance.
- 2026-09-02 Maix App startup diagnosis slice: a Launcher start of `0.1.4` loaded the App modules but exited before a resident Camera process or fresh health state appeared; the Launcher did not retain its stdout/stderr. App `0.1.5` now records private phase breadcrumbs and bounded uncaught Python tracebacks in `app-startup-diagnostics.jsonl`, without recording configuration or credentials, and is installed through `app_store_cli`. A reboot-first single-Launcher-start check is required to distinguish a Python exception from a native multimedia exit; this is diagnostics deployment, not App launch acceptance.
- 2026-09-02 Maix App package-source correction: the new startup diagnostic identified the actual `0.1.5` failure before Camera initialization: the package manifest copied a stale App-local `health_daemon.py`, whose constructor lacked the current media-memory arguments. The manifest now packages the authoritative Camera `health_daemon.py` and `person_presence.py`; a regression guards those mappings. `openhalo_camera_edge` `0.1.6` was installed through `app_store_cli`, and its packaged health daemon byte-matches the current development module. The next Launcher start is required for runtime acceptance.
- 2026-09-02 formal Maix App launch check: the owner started only `openhalo_camera_edge` `0.1.6` from Launcher after the package correction. The resident process is the formal `/maixapp/apps/openhalo_camera_edge/main.py`, it reached `daemon_built`, connected to Runtime, and over a follow-up interval continued to seal IDR-safe H.264 segments, package MP4s with `ffmpeg` return code `0`, and admit Hot Ring artifacts. This accepts the corrected formal App launch plus local capture/Hot-Ring liveness; provider Understanding/action-result and ordinary-chat completion remain separate acceptance work.
- 2026-09-02 media-query timeout diagnosis hardening: a post-launch normal chat (`interaction-87`) correctly dispatched `media.memory.query` (`action-11`) to the formal Camera Edge, but Runtime received no Camera action result before its wait elapsed and returned a safe inability response. App `0.1.7` now records local query receipt/result-send stages and non-secret Provider request stages, enforces a 30-second end-to-end Provider request ceiling below the Runtime wait window, and bounds TLS close waiting. Focused adapter/Camera/App regressions pass; the installed package awaits a new Launcher start and real media-query retry, so provider Understanding remains unaccepted.
- 2026-09-02 Provider failure diagnosis follow-up: the next live query (`action-14`) reached the configured Edge Provider and returned a correlated `media_understanding_unavailable` action result, so registration, action routing, Hot Ring selection, and result delivery are live; the provider request itself failed after start but before response parsing. App `0.1.8` adds a bounded, non-secret `request_failed` diagnostic (exception class plus a single-line 512-character message) so the next retry distinguishes TLS/connectivity, HTTP, and response-shape failure without logging credentials, headers, or video payloads. Focused regressions pass; successful Provider Understanding remains unaccepted pending that real retry.
- 2026-09-02 Provider HTTP diagnosis follow-up: the `0.1.8` retry proved the provider returned a non-2xx response rather than timing out or losing the action result. App `0.1.9` now includes only the HTTP status and a strict short machine error-code token in the local failure diagnostic; it still excludes response prose, credentials, headers, and media. Focused regressions pass. The next real retry must identify whether the remaining defect is authentication, entitlement, or request compatibility before a provider-format change is made.
- 2026-09-02 Provider HTTP framing follow-up: the first `0.1.9` retry returned `HTTP 400`, but its error body used standard HTTP/1.1 chunked transfer framing, which the minimal Edge transport had not decoded and therefore classified as `unparseable`. App `0.1.10` decodes bounded chunked response bodies before parsing either success JSON or error codes; this is required for compatible endpoints that chunk normal replies as well as errors. Focused regressions pass. The next retry will expose the provider machine code for the existing 400 request before changing its media payload contract.
- 2026-09-02 Provider request-compatibility diagnosis: the `0.1.10` retry decoded the response and identified `HTTP 400 invalid_parameter_error`; official Qwen documentation confirms the selected `qwen3-vl-flash` supports video input, `video_url` Base64 Data URLs, and `enable_thinking=false`, so the remaining error is a concrete request/media validation defect rather than a model-capability or transport failure. App `0.1.11` records a short provider explanation with credential, Bearer token, and Base64 Data URL redaction; focused regressions pass. Its next live query is needed to correct the rejected field precisely.
- 2026-09-02 Camera timing correction: the `0.1.11` provider explanation identified the rejected field as a video file that was too short. Device evidence showed a nominal 5 FPS stream sealed over about 3.5 wall-clock seconds but containing only five encoded packets; the old MP4 mux used the nominal FPS and emitted a one-second file. App `0.1.12` derives the ffmpeg input rate from observed packet count and wall-clock duration, records that timing in segment diagnostics, and therefore preserves the actual interval for Provider validation. Focused regressions pass; the next live query must verify a Provider Understanding response.
- 2026-09-02 Camera short-window video-understanding acceptance: after the `0.1.12` timing correction, two normal Runtime chats dispatched `media.memory.query` to the formal Maix App; the Edge selected local Hot Ring MP4s, invoked the configured Qwen Provider, and received non-empty Understanding responses (260 and 141 characters) in about 22 and 25 seconds. Runtime passed the returned text into its post-action Main Hermes turn, which rendered the grounded scene description to the Android Edge through the normal `notification.show` action. Current device diagnostics continue to show independently decodable MP4 segments with observed container rates near 1.3--1.5 FPS over about 3.4--3.9 seconds. This accepts the bounded short-term video path (`normal chat -> Runtime action -> local Hot Ring -> direct Provider -> textual Understanding -> Main Hermes -> interactive Edge`), not long-term Episode memory, audio memory, high-frame-rate capture, latency targets, failure recovery under long-duration operation, or full M17.10 acceptance.
- 2026-09-02 Camera media-query latency baseline and general Runtime target: two successful physical end-to-end queries measured about 94--104 seconds from user input to final Android response (about 99 seconds mean). The currently deployed `Cubence / gpt-5.6-terra` Main Hermes route consumed about 35--37 seconds for the initial action decision and about 30--38 seconds after the video Understanding to form/present the final answer; the Camera Edge dispatch/selection stage took about 5--7 seconds and the direct DashScope `qwen3-vl-flash` video request about 22--25 seconds. This is an observational baseline, not a latency acceptance. After Camera capture/Hot-Ring long-duration stability is established, the next performance objective is a comfortable roughly five-second end-to-end Runtime interaction path for ordinary bounded request -> action -> result interactions. This is explicitly a general Runtime/Agent-execution performance program, not a Camera-specific or phrase-specific fast route: it must reduce unnecessary model round trips for all registered Actions, preserve the same contracts, Presence and execution validation, and let Main Hermes retain semantic continuity without acting as a mandatory serial rewrite step before and after every action. Camera video questions remain a useful stress case; multi-second cloud-video reasoning may exceed the ordinary five-second budget and must return a truthful progress/deadline state rather than silently degrading the entire interaction path. Provider-level first-byte/response telemetry remains required before attributing the observed Hermes delay solely to Cubence network, model, or queue behavior.
- 2026-09-02 general fast interaction execution direction: the intended five-second path is not a list of capability-specific heuristics. Runtime should first establish measured p50/p95 stage telemetry and deadline propagation across ingress, Main Hermes/provider invocation, action dispatch, Edge execution, result correlation and presentation. It should then make generic interaction-loop improvements: retain/reuse compact bounded context rather than repeatedly rebuilding expansive prompts; permit one governed Action plan to dispatch without a second full model pass when a registered Action result already carries presentation-safe bounded Understanding; and use an explicitly selected low-latency Runtime model/provider or persistent model session for ordinary Action planning and result handling, with Main Hermes escalation reserved for reasoning that genuinely needs it. Each Action's semantic and machine contract—not Camera-specific code—must declare whether its successful result is eligible for bounded direct presentation, needs model interpretation, or needs follow-up Action planning. Typed Camera Features and still/video evidence remain Edge implementation choices behind these generic contracts. No raw media enters Runtime, and a five-second claim requires physical acceptance of the entire generic path rather than inference from the present Qwen-video request time.
- 2026-09-02 immediate Episode plus Hot Ring media-memory decision: long-term semantic memory must not wait for media to leave the five-minute Hot Ring. While closed local segments remain Hot Ring-resident and evidence is freshest, a bounded asynchronous Episode Worker is triggered by confirmed local Feature/transitions or low-frequency stable-scene sampling; it deduplicates/merges adjacent candidates and writes one deterministic `EpisodeRecord` plus a Markdown `EpisodeNote`. The record, not model JSON, owns source/time/clock uncertainty, coverage/gaps, Feature references, raw-evidence availability/pin state, model provenance and generation/sync/deletion status. Runtime receives and indexes only that envelope/note, then compiles a bounded natural-language projection for Main Hermes; raw MP4/frames remain local. Hot Ring serves detailed/temporal verification, Episode retrieval serves ordinary recall even after Hot Ring expiry, and unavailable raw evidence must produce an explicit limitation rather than a guessed detail. Only pending/failed/offline raw intervals move to a separately bounded Archive Ring as Hot Ring rotates; it records `coverage_gap` on eviction and can never impair capture or Hot Ring. This remains an unimplemented single-source first phase before multi-Edge source isolation/correlation acceptance.
- 2026-09-02 Camera Provider registration negotiation: media-capable Edge registration now advertises optional `model_requirements`; Runtime replies with an independent `device_configuration` frame instead of routing Provider provisioning through the ordinary Action/Interaction lifecycle. Edges without model requirements retain the unchanged registration path, and configuration acknowledgement is handled as a control-plane result.
- 2026-09-03 Camera Edge hardware validation decision: use a Jetson Orin Nano Super 8GB development kit as the first practical host for the room-scale multimodal Edge prototype, paired with a separately verified 4K camera and the bundled audio input/output hardware where its Linux/Jetson interfaces are confirmed. The September acceptance target is a complete local sensing -> change detection -> bounded visual/audio understanding -> Runtime context integration loop, with a post-change semantic result targeted within five seconds; this is a validation target, not yet a production hardware lock or a claim of error-free identity/activity understanding. Orin Nano 2 remains a future option and is not available for the current validation window.
- 2026-09-04 Camera Edge semantic-label target: the five-second post-change result is a short free-form natural-language event label, not only a fixed activity class. It remains bounded evidence with source, interval, coverage, confidence, model provenance, and uncertainty; Runtime combines it with context and retains authority over state admission, Presence, and action.
- 2026-09-04 Camera Edge version boundary: the received MaixCAM work is explicitly Camera Edge v1, a completed narrow protocol/Feature validation that demonstrated the platform cannot meet the room-scale high-resolution perception requirement. The separate Jetson Orin Nano Super 8GB effort is Camera Edge v2, a new validation line for 4K room capture, multimodal sensing,常驻 change detection, and five-second free-form natural-language event labels; v2 must not be reported as an extension, acceptance, or hardware replacement of v1.
- Camera Edge v2's separate September hardware, processing, evidence, and acceptance design is [Camera Edge v2 — Jetson Orin Nano Super Validation Design](docs/plans/2026-09-04-camera-edge-v2-nano-super-design.md).
- Proposal formation should be allowed to synthesize an interaction hypothesis from one high-salience signal or from multiple device observations together; a line of terminal text is only one possible trigger shape, not the only interaction origin the runtime may reason about
- Model-backed proposal formation may emit interaction-semantic candidates such as interaction type, candidate participant surfaces, visibility intent, and the current `primary action`, but those remain proposal-layer candidates until presence governance and execution planning turn them into an actual runtime outcome
- `Presence Router` should act as an explicit governance and adjudication layer, not only a passive allow/block filter: it may suppress, narrow, retime, or redirect proposed surfaces and actions based on policy, privacy, activity, capability, and availability constraints
- `InterventionProposal.source` records the Runtime interaction phase (`sense_first`, `agent_initiative`, `observation_driven`, `post_action`, or `post_observation`), never the embedded model implementation. The current stage intentionally has no global intervention-history cooldown: terminal activity, contextual ambiguity, capability, privacy, permission, and explicit policy remain active governance inputs; future rate control must return as evidence-backed scoped policy work rather than a fixed global timer.
- The runtime may keep one `primary action` per planning turn in early slices, but that bound should remain an implementation constraint rather than a long-term architecture rule; the same interaction lifecycle should later support multi-turn `action loop` re-entry after action results or new observations
- M20 action-loop remediation replaces the historical one-external-action assumption with a runtime-owned `ActionBatch`: Hermes may complete bounded internal reasoning and propose one or more distinct governed actions, Runtime validates and dispatches the complete batch, and the interaction remains `awaiting_action_results` until every exact correlated action result, timeout, or structured failure is recorded. Distinct valid actions are never silently collapsed into `no_intervention`; exact duplicates are folded idempotently and invalid or conflicting batches terminate with an explicit inspectable rejection.
- `InteractionPool` is an internal sibling module of `Proposal Formation`, `Presence Router`, and `Execution Planning` inside `Agent Runtime`; it is not a new top-level process/lifecycle domain and is not part of the Hermes implementation itself. It owns interaction lifecycle, turn/action-result correlation, and re-entry state, while `RuntimeOrchestrator` coordinates continuation dispatch and reawakening of the same Hermes child session. Hermes-native child sessions perform semantic work, while Runtime remains authoritative for concurrency, context projection, Presence, executor validation, result correlation, timeout, recovery, and completion.
- The canonical physical edge term for a device that combines camera, microphone/audio, speaker, and other local modalities is `Multimodal Edge` (能力仍以独立 capability、权限和隐私契约注册；不把设备身份拆成多个 Edge）。
- `Multimodal Edge` is a later composition step, not the immediate implementation target. The delivery order is: first complete and independently accept `Camera Edge v2`, then build and independently accept a standalone `Audio/Microphone Edge`, and only afterward compose their capabilities into one Multimodal Edge where the hardware and lifecycle boundary make that worthwhile. The two standalone Edges must remain usable and diagnosable without the composition layer.
- `RuntimeOrchestrator` is the only deliberative proposal coordinator for every non-direct path: normal user turns, observation-driven admission, post-observation re-entry, post-action/result re-entry, and agent initiative must all pass through its snapshot/context-to-harness/proposal-to-Presence-to-execution sequence. `InteractionPool` may register, correlate, request continuation, and provide bounded lifecycle summaries, but it must not own an independent proposal or action shortcut. The explicit direct-action path remains the sole deliberate exception.
- The intended interaction worker model is an ordered per-Interaction semantic continuation worker/child session behind the Orchestrator. A generic `asyncio.to_thread` used to keep synchronous frame/model work off the event loop is only an execution mechanism, not an architectural proposal path. The current code's scheduler/child-worker design remains a wiring and acceptance concern until live Runtime ingress actually routes through it; regardless of implementation mechanism, child continuation must return to `RuntimeOrchestrator` before proposal, Presence, validation, or action dispatch.
- Hermes-native child sessions have isolated conversation histories. Before initial work and every result-set continuation, Runtime supplies a bounded shared-context projection containing the OpenHalo identity contract, relevant durable MemoryStore facts, active goals, exact device roster, and interaction lineage/results. This is not a copy of the persistent main-agent transcript; Runtime records lifecycle/audit pointers while Hermes MemoryStore remains the semantic durable-memory authority.
- Model-native tool calls, MCP tool calls, runtime-local tools, skill/procedure invocations, and external device actions should converge into one runtime-owned action intent/result model before side effects occur; provider-native tool syntax is an adapter input, not a permission to bypass OpenHalo action governance
- `Personal Runtime` is the authoritative source of a bounded structured device roster projected from registered device identity, capability contracts, and live availability. The Agent Harness receives that roster and performs semantic target selection from it; `Runtime` validates and governs the selected exact device target but must not replace it through keyword routing or another semantic fallback.
- The executor kind for a model-native action is selected by the OpenHalo adapter or action registry, not by a model-supplied tool argument; the M20 `openhalo_action` bridge is limited to governed `Device Edge` actions, while runtime-local, MCP, and skill/procedure routes remain OpenHalo-owned registrations
- Agent behavior should be constrained by explicit prompt/context contracts, behavior contracts, capability/action registry validation, and post-generation validation or repair before any user-visible or side-effectful action is executed
- The long-horizon interaction-intelligence direction is a multimodal, time-aware model that may interpret continuous vision, audio, text, screen, and device evidence into interaction candidates such as `silence`, `observe_more`, `intervene`, or `delegate`; it must stop at a candidate boundary and never bypass Runtime-owned Presence, privacy, permission, routing, action validation, result recording, or feedback handling
- Personal multimodal data is user-owned and remains local to the relevant Device Edge and Personal Runtime by default. A future shared training corpus may contain only explicitly consented, reviewable, minimized interaction traces with documented provenance and licensing; it must not be built through default raw audio, video, or screen telemetry collection
- Presence policy should remain explicit and inspectable even when model-generated or model-repaired; models are not the only durable representation of proactive behavior
- A host-resident edge running on the runtime's own server is still modeled as a first-class `Device Edge`; physical co-location does not waive the `Edge Session Link <-> Gateway` boundary
- The runtime should support both a normal deliberative path and an explicit edge-requested fast path for direct actions
- A direct action fast path may bypass the normal `Agent Runtime` path, including `Presence Router`, but it must still pass through `Gateway`, validate structured input against the exact registered target capability/schema, restrict `runtime.*` actions to the explicit runtime-control allowlist, update runtime state/context, and record action results; on the normal path, a valid `runtime.* -> runtime.control` mapping remains subject to all ordinary Presence, modality, privacy, and schema filtering
- Runtime feedback interpretation should treat `ignore != negative`; explicit rejection or repeated similar-context dismissal should carry more weight than one-off non-response
- Presence policy updates should optimize for both immediate user experience and likely future user experience, rather than greedily maximizing the current interaction outcome
- For the first same-template multi-edge slice, ordinary routed actions should prefer a different online edge instance with the required capability before falling back to the source device
- Ordinary development work should be branch-first in the main workspace and should reuse the repository root `.venv` by default, while optional worktree-based dependency or packaging experiments should use an explicitly created worktree-local `.venv`
- Runtime startup should distinguish restart-heavy development acceptance from the installed personal Runtime: development helpers use port `18765` by default, while an installed Runtime is started and supervised as an implementation detail by the global `openhalo` command. Product users have one personal installation model, not a choice between visible system-service and personal modes. The obsolete `systemd` Runtime deployment has been retired; the current product deployment is the personal command/data model only.
- The personal installation owns one durable home, `OPENHALO_HOME` (default `~/.openhalo`), containing personal configuration, device identities, pairing records, state, and logs. Immutable program releases live separately under `~/.local/share/openhalo/releases/<commit>` with an atomically selected `current` release, so a program update never silently discards personal Runtime or Edge data.
- The host-class `Device Edge` is lifecycle-owned by its colocated `Personal Runtime`: after Gateway is listening, Runtime starts and supervises one loopback WebSocket Host Edge using the normal public Edge API. Host Edge startup or reconnect failure is not a Runtime-fatal condition; its supervisor must retry continuously with bounded backoff, preserve an inspectable unavailable/retrying state, and stop the edge task when Runtime stops. Physical colocation does not permit direct backend calls or removal of the `Edge Session Link <-> Gateway` boundary.
- Gateway binds each real WebSocket to exactly one successfully authenticated `device_id`: post-connect frames must carry that same identity, unauthenticated frames receive `not_connected`, cross-device frames receive `device_mismatch`, and a second live socket cannot silently replace an existing device session. The current pairing registry implements one-time-code issuance, device-specific bearer credentials, hash-only Runtime persistence, revocation, and restart survival; it is an accepted development compatibility baseline, not the product authentication target. Product Edges must use a one-time pairing code only to register a device public key, then authenticate every later Edge Session Link through a Runtime nonce challenge signed by that device's private key. The signed challenge binds the canonical Runtime audience, `device_id`, nonce, and expiry. Product mode must reject bearer device credentials and legacy shared tokens rather than retaining either as a fallback. Product Edge endpoints must support both `ws://` and `wss://`; plaintext `ws://` is a first-class, preferred owner connection path and must not be rejected merely because it uses a public IP address or lacks a domain, while `wss://` remains an optional compatible transport. P-256 proof, pairing, revocation, and audience binding remain mandatory for both schemes; `ws://` intentionally does not promise transport confidentiality, so the endpoint UI/CLI must state the selected scheme without turning it into a blocking TLS policy. An Edge persists only its Runtime endpoint, public identity metadata, and protected private key material, while Runtime persists public-key/device metadata, revocation state, and audit-safe pairing facts. For the first cross-platform product slice, an Edge stores its private key as `$OPENHALO_HOME/devices/<device_id>/identity.ed25519` (default root `~/.openhalo`), with parent directories `0700` and the file `0600`; it never enters `config.json`, logs, diagnostics, exports, or pairing receipts. Native system-keychain storage may later wrap or migrate this key, but is not a first-slice requirement. The detailed pairing ceremony, endpoint trust, rotation, recovery, and migration rules remain a dedicated design pass before implementation.
- CLI device validation is acceptable for early module testing, but host-edge verification is required before documenting a module as fully implemented and operationally ready
- In this project, `manual acceptance` or `human acceptance` means testing implemented functionality in a simulated real usage scenario, rather than only checking static output, isolated unit behavior, or non-interactive script success

Initial productization target:

- The first productized OpenHalo slice should package three surfaces together rather than treating them as unrelated developer processes: phone `Device Edge`, desktop/computer `Device Edge`, and server-side `Personal Runtime` plus host-class `Device Edge`
- OpenHalo remains one source monorepo while its product surfaces have independent distribution lifecycles. Shared source, protocol definitions, pairing semantics, and end-to-end regression stay together; Runtime and each Edge are separately installable, version-selectable, and updateable deliverables rather than separate GitHub repositories or one all-or-nothing installed program.
- OpenHalo exposes one personal-owner installation model across these surfaces: one global `openhalo` command for Runtime control and one `~/.openhalo` data home by default. A background Runtime/Host Edge supervisor may exist internally, but setup, start, status, logs, pairing, revocation, diagnostics, update, and rollback must not require the user to choose or operate a system-service mode.
- The Linux Runtime should support one primary install command or script that installs the globally available `openhalo` command and configures both `Personal Runtime` and the co-installed Host Edge under the personal owner, while still preserving the `Device Edge -> Edge API -> Gateway -> Personal Runtime` boundary
- Terminal Edge should be independently installable through one primary user command and expose `openhalo-edge`; its connection and device credential remain in the same owner-controlled `~/.openhalo` home without requiring root or a second configuration model
- Runtime and Terminal Edge program lifecycles must be independent even when both are installed on one machine: they use separate immutable release roots and separate update/rollback commands. `openhalo update` may stage, switch, and restart only Personal Runtime plus its managed Host Edge; `openhalo-edge update` may stage and switch only Terminal Edge. Neither command may replace, restart, or roll back the other surface. Pairing and owner data remain durable local data across either program update, and a later compatibility policy must reject or warn on unsupported Edge/Runtime protocol combinations rather than coupling release cadence by default.
- The Windows desktop edge should be installable through a normal user-facing installer rather than only through a development shell; the productized desktop package may include the runtime and host edge as optional local components that are installed but disabled by default
- The Android phone edge should be deliverable as an APK suitable for real-device installation outside Android Studio
- The standard deployment scene is: one public server running `Personal Runtime + host edge`, one computer running the desktop edge, and one phone running the phone edge
- The computer-server deployment scene is: one computer running `Personal Runtime + host edge + desktop edge`, with the phone edge connecting to the computer-hosted runtime
- Program releases and durable personal data are separate. The fixed-commit installer atomically selects `current` for bootstrap. The first GitHub Release update slice now provides `openhalo update --check`, `openhalo update`, and `openhalo rollback` from an installed immutable release: it resolves the latest non-prerelease Release only when its `release-manifest.json`, `SHA256SUMS`, and archive agree on one 40-character commit and SHA-256; stages the program privately; stops a running Runtime only after staging; atomically selects the candidate; starts it with the candidate Python; and restores the preceding release/Runtime if it does not become ready. It never installs directly from mutable `master` or resets `OPENHALO_HOME`. This is an unsigned GitHub-HTTPS/checksum baseline that verifies the published OpenHalo archive, not yet manifest-signature verification, hash-locked dependency wheelhouse, or persistent-state migration support; releases must preserve compatible state until an explicit migration/recovery design is implemented.
- M22 product blocker recorded from user feedback: the current manual `stop -> fixed SHA installer -> version check -> start` sequence is bootstrap or recovery plumbing, not an acceptable normal update experience. The ordinary owner path must be one `openhalo update` command with clear progress and outcome; it must select a verified immutable release, preserve compatible `OPENHALO_HOME` data, stage and health-check before activation, restart only after a successful switch, and automatically restore the previous release on failure. Users must not need to handle commit IDs, release paths, symlink switching, runtime PIDs, or service-management choices during a normal update.
- Product packaging is now an explicit product milestone, not only a release-engineering afterthought; UI polish, installation flow, hidden per-user supervision, endpoint pairing, safe release switching, and deployment clarity all count as part of the first productized slice

## Edge Representation Model

For edge integration, the working direction is a layered model rather than a flat "all hardware is the same kind of node" model.

- `Device` is the system-level identity and constraint layer
- `Capability` is the runtime-facing contract layer

In this model:

- Every edge participant is represented as a device with identity, connectivity, trust, placement, power, and resource constraints
- Each device may expose one or more capabilities such as sensing or actuation functions
- Runtime task selection and routing should prefer capability-level reasoning
- Safety, availability, permissions, and performance constraints remain anchored at the device layer

This direction is meant to support a wide range of edge classes, from full computing surfaces such as phones, desktops, and Raspberry Pi nodes to constrained controllers such as ESP32-class devices, without forcing the whole system to collapse to the lowest common denominator

The current preference is to support graded edge roles through device profiles rather than forcing one uniform execution shape for every device class. The design constraint is that profile modeling must stay small and legible rather than becoming a large matrix of ad hoc per-device exceptions

The current preference for profile modeling is:

- Primary classification by system role
- Secondary description by device type
- Resource scheduling detail can remain intentionally lightweight until it becomes a real product bottleneck

This means profile shape should likely answer "what role does this node play in the runtime" before answering "what hardware family is it"

For role modeling, the current preference is to keep the role set intentionally small. When onboarding new device classes, the default should be to reuse an existing role whenever possible and only introduce a new role when reuse clearly breaks down


## Core Goals

### Goal 1: Define the system architecture clearly

We need a stable high-level architecture before deep implementation begins.

Sub-goals:

- 1.1. Define the overall system boundary
- 1.2. Define the backend module boundaries
- 1.3. Define the frontend/backend contract
- 1.4. Decide whether OpenClaw gateway should be reused as an isolated control-plane component or kept only as reference material

Acceptance criteria:

- A written architecture description exists
- The role of `Device Edge` and `Personal Runtime` is explicitly separated
- The meaning of `Gateway`, `State`, `Agent Runtime`, `Presence Router`, and `Action Layer` is documented
- A clear decision exists on whether OpenClaw gateway code is reused directly, selectively referenced, or replaced

Status:

- Completed

Implementation note:

- The implementation path is no longer only conceptual; the first v0 batch has been started and the scaffold/protocol/state foundations are now in place

### Goal 2: Establish the project's primary abstractions

We need a stable and explicit abstraction baseline for the project so later implementation work does not drift back toward a `channel -> session -> agent` product shape.

Sub-goals:

- 2.1. Confirm first-class abstractions for the new system
- 2.2. Downgrade legacy abstractions to implementation details where appropriate
- 2.3. Define the minimum state model needed for continuity
- 2.4. Define the runtime dispatch-path abstraction for deliberative handling versus direct edge-requested action handling
- 2.5. Research and define the presence-policy model, including policy representation, scope boundaries, conflict handling, lifecycle management, and feedback-driven refinement

Acceptance criteria:

- Primary runtime abstractions are documented explicitly as `device`, `capability`, `context`, `agent`, and `action`
- `channel` and `session` are explicitly classified as secondary implementation concepts rather than primary product abstractions
- The project documents that `Presence` remains an explicit, inspectable governance module inside the broader agent runtime rather than disappearing into opaque agent behavior
- The minimum state model includes context, device state, handoff state, intervention history, experience feedback signals, and tasks as a derived or optional structured object rather than the mandatory primary axis
- The runtime documents which layers may be bypassed by an explicit direct-action request and which state/context recording steps remain mandatory
- The project documents that proactive behavior is agent-centered but presence-governed, with explicit or inspectable presence policy as the durable control surface for intervention decisions
- The project documents that early presence-policy refinement should use a review-gated update loop: runtime and agent tooling may prepare policy update candidates from feedback, but user-approved review remains the default before changes take effect
- The project documents that presence decisions should consume structured context observations and a synthesized context snapshot rather than raw environment signals directly
- The project documents that the shared observation vocabulary should grow incrementally at the top level: new terms are introduced during edge development, validated centrally, and then used as normal runtime vocabulary once accepted
- The project documents that proactive runtime evaluation may be initiated either by edge/context signals or by agent initiative, but both paths must converge on the same presence gate

Status:

- Completed

Implementation note:

- This goal is intended to finish once the abstraction vocabulary is written clearly enough that later implementation work can be checked against it, while the anti-drift rule itself remains a standing architecture constraint. The current direction is to treat `agent` as the primary intelligent actor, treat `presence` as an explicit internal governance layer for intervention decisions, treat `task` as a secondary structure that may be created when useful, and allow agents/models to grow or repair inspectable presence policy from feedback over time.
- The current implementation preference is to keep the online runtime path small and inspectable: edge mappers produce normalized runtime observations, lightweight per-observation reducers synthesize compact context snapshot fields, and `unknown` or `ambiguous` results are allowed when evidence is insufficient.
- The current implementation preference is to keep presence evaluation unified even when the trigger differs: edge/context activity and agent-initiative checks should both build or refresh compact context snapshot state and then flow through the same presence decision surface.
- The current implementation preference is to model `Agent Runtime` as one coherent backend module whose internal flow includes proposal formation, explicit presence governance, and later execution planning, rather than splitting those responsibilities into multiple top-level architecture boxes.
- When edge-delivered signals carry user intent, the current design preference is to keep `Agent Runtime` proposal formation on a small runtime-native taxonomy: normal user-visible or side-effectful outcomes are `action`, silent suppression/closure is `no_intervention`, and model/provider failures use the internal `provider_failure` channel rather than chat-style `reply` or `clarification` top-level proposal types.
- The current implementation preference is to avoid duplicate context-carrier layers on the hot path: if `grounding bundle` feeds a `prompt/context package`, later steps should consume that package directly rather than rebuilding equivalent payloads under new names.
- Inspection-oriented surfaces such as behavior contracts, replay/eval reports, or other verification artifacts should be treated as sidecars around the hot path by default; they may validate or summarize live-chain artifacts, but should not become mandatory intermediate decision objects unless they directly change runtime behavior.
- The detailed presence-policy design remains intentionally deferred for a dedicated research and design pass. That pass should explicitly study policy representation shape, conflict avoidance and resolution, orthogonality of policy scope, short-term versus long-term policy lifecycle, how present user-experience optimization should be balanced against future user-experience impact, how policy update review cadence should lengthen as the system becomes more stable, how environment understanding should flow from raw edge signals into structured context observations and then into a presence-consumable context snapshot, and how the shared observation vocabulary should be extended safely as new edge types are added.
- Heuristic-learning style improvement should live in one unified outer maintenance loop rather than the hot decision path: feedback, replays, and tests may drive coordinated updates to edge mappers, observation reducers, vocabulary, and presence policy, but those changes remain review-gated before entering the normal runtime path.
- Short-term device-context integration should use built-in observation-to-snapshot reducers for first-product core edges such as phone, desktop/terminal, host, and runtime health surfaces, so their presence-relevant observations become stable decision inputs without waiting for a generic dynamic mapping system.
- Before expanding into broad multi-sensor edge deployments, the project should implement the heuristic-learning governance loop that can inspect observations, snapshots, decisions, action results, and feedback, then propose reviewed mapper/reducer/vocabulary/presence-policy updates for the larger observation space.
- Presence should consume the compact context snapshot directly rather than introducing an additional presence-only feature view; richer observation evidence remains available separately for agent reasoning and debugging when needed.
- The current design preference is that raw fine-grained device history remains edge-local by default: core stores normalized observations plus provenance, while deeper agent inspection of device history should use explicit bounded edge-side diagnostics or history retrieval instead of continuous raw-history duplication into backend state. Terminal Edge Coding activity uses an append-oriented, paged local journal: a simultaneous-active-task limit is only a resource guard, active task history is never truncated by event count, the UI loads bounded pages, and only completed-task history is reclaimed by configurable local capacity policy.
- Normalized Terminal Edge Coding activity enters Personal Runtime through the registered ordinary observation ingress. Coding source names carry vocabulary and provenance only; they do not activate source-specific suppression, priority, emphasis, or `record_only` lifecycle behavior. Generic privacy, retention, context, relevance, Agent Harness, Interaction Pool, and Presence policy apply uniformly. Individual Runtime observations remain bounded summaries or local evidence references and never become a continuous raw reasoning, full-output, or full-diff mirror.
- Agent initiative should be a first-class high-salience input to presence evaluation rather than a low-priority afterthought, but it should still remain subject to suppression, privacy, and timing policy.
- Runtime interaction lifecycle should be source-neutral through one bounded `InteractionPool` inside `Agent Runtime`: explicit user events, admitted observation-driven triggers, agent initiative, and later action-result or fresh-observation re-entry all register or resume ordinary interactions rather than using source-specific lifecycle paths. `M18` may decide whether passive evidence merits registration, but once registered its interaction has the same proposal, presence, action, result-routing, and completion semantics as a chat-originated interaction. A separate top-level `ContinuationRouter` is not part of the architecture; continuation coordination remains an internal `RuntimeOrchestrator` responsibility over `InteractionPool` records.
- The Interaction Pool should support multiple active interactions concurrently. It may deduplicate or merge only the same causal/idempotency scope, identified from triggering evidence and provenance rather than time proximity or a guessed intent; unrelated interactions may deliberate in parallel, while `Presence Router` remains the common user-facing delivery arbiter.
- The current bounded M18 Gate is a provisional fixed-signal implementation, not the final definition of observation understanding. The durable M18 direction keeps deterministic privacy, provenance, causal, deduplication, and budget safeguards at the observation boundary, but lets the Agent Harness actively modulate relevance and follow-up from its current working/semantic/episodic state, uncertainty, active interactions, and user situation. This must not collapse into a fixed catalog of profile fields or questions: the harness may decide that an observation deserves deeper attention, request bounded safe evidence, defer it, or leave it as context according to the present situation, then register any resulting work through the ordinary Interaction Pool and shared Presence governance chain.
- Reference inspiration for this outer-loop direction: [Learning Beyond Gradients](https://trinkle23897.github.io/learning-beyond-gradients/#zh)

### Goal 3: Define the initial implementation path

We need an implementation starting point that is focused enough to produce progress quickly.

Sub-goals:

- 3.1. Select the first project folder and documentation baseline
- 3.2. Define the first implementation milestone
- 3.3. Identify the minimum backend modules for v0
- 3.4. Identify the minimum device surfaces for v0
- 3.5. Define the first post-v0 multi-edge slice using the same edge template on more than one device instance

Acceptance criteria:

- The project has a dedicated folder
- Project baseline documentation exists
- The first milestone is small enough to implement without solving the whole system
- The v0 scope names which modules and device surfaces are first-class
- The next slice after v0 names how multiple same-template edges participate and what routing behavior it is meant to validate

Status:

- Completed

### Goal 4: Build the project incrementally from architecture to runtime

Goal 4 owns the milestone ladder from the initial architecture baseline through runtime execution, multi-edge expansion, observation understanding, productization, and later ecosystem integration.

The complete milestone definitions and acceptance criteria are maintained in the [milestone acceptance matrix](/root/openhalo/docs/plans/milestone-acceptance-matrix.md). `Project.md` keeps only the route and current status so the Agent can load detailed criteria when a milestone becomes active.

Current milestone groups:

- `M0`-`M16`: architecture, runtime, context, presence, model, terminal, and action-loop foundations; accepted baseline.
- `M17.0`-`M17.7.2`: public Edge API and Android/multi-edge foundations; accepted baseline.
- `M17.8`-`M17.11`: current Device Edge expansion under parent Issue `M17`.
- `M18`-`M19`: observation understanding and bounded storage; `M18`, `M18.1`, and `M19` accepted.
- `M20`-`M20.3`: Harness, action governance, progress presentation, and stable Terminal Edge; accepted baseline.
- Hosted Coding Bridge adapter ([#14](https://github.com/nwomn/openhalo/issues/14)): accepted cross-cutting Terminal Edge capability; it does not create a new milestone or alter the active `M17.8` route.
- `M20.1`, `M21`, `M22`, `M23`, `M24`: later procedural-memory, policy-learning, productization, ecosystem-bridge, and independent cellular Camera Edge route.

Goal-level acceptance rules:

- Each milestone has a clear scope and explicit acceptance criteria in the matrix.
- Each milestone can be accepted independently, with dependencies recorded in its definition.
- A milestone is not active merely because it exists on the roadmap; it becomes active when its Issue or child Issue is ready for implementation.

Status:

- In progress; the current execution target is `M17.11` / Issue `#19` Proxy Interaction Edge baseline. `M17.8` mobile sensitive-screen capture governance and `M17.9` native Windows Desktop Edge remain deferred `Backlog` work for this execution window.

### Goal 5: Productize OpenHalo into an installable three-end system

We need OpenHalo to become a coherent product that can be installed, configured, connected, and tried without requiring the user to manually run unrelated developer processes.

Sub-goals:

- 5.1. Define one personal-owner deployment shape across phone edge, desktop/computer edge, and Runtime/Host Edge, with `OPENHALO_HOME` defaulting to `~/.openhalo`
- 5.2. Provide a Linux personal Runtime installation path that installs and internally supervises `Personal Runtime + Host Edge` together through the global `openhalo` command
- 5.3. Provide a Windows desktop edge installer that can connect to a remote Runtime and can optionally include disabled-by-default local Runtime/Host Edge components under the same personal-owner model
- 5.4. Provide an Android APK delivery path for the phone edge outside Android Studio
- 5.5. Define first-run setup, endpoint pairing, connection health, diagnostics, and recent-activity UI expectations across phone and desktop surfaces
- 5.6. Provide staged immutable-release updates, safe rollback, and explicit persistent-state migration handling without data loss
- 5.7. Verify the two accepted deployment scenes: standard public-server deployment and computer-server deployment

Acceptance criteria:

- A written productization baseline exists for the first installable OpenHalo slice
- The accepted deployment scenes are documented clearly enough that a user can tell what machines and packages are required
- Linux Runtime/Host Edge installation can be performed through one primary command or script and results in hidden personal supervision with `openhalo` status/log visibility
- Windows desktop edge installation produces a user-facing installed app, not only a development-shell entrypoint
- Android phone edge delivery produces an installable APK preserving the accepted daily-use phone-edge behavior
- Phone, desktop, runtime, and host edge all continue to communicate through the public Edge API boundary; packaging must not introduce hidden backend shortcuts
- Program updates are verified immutable releases that stage before activation, retain the prior executable release for rollback, and preserve compatible `~/.openhalo` data; unsupported state migrations stop explicitly rather than silently resetting data
- Manual acceptance demonstrates both standard public-server deployment and computer-server deployment using packaged or packaging-equivalent artifacts

Status:

- In progress (`M22` is the first concrete implementation milestone for this goal; staged update/rollback is part of this packaging baseline, while broader product polish, app-store distribution, account/login UX, and encrypted local secret storage remain later hardening)

### Goal 6: Build toward user-sovereign multimodal interaction intelligence

OpenHalo's long-horizon product direction is an interaction model that can remain appropriately present across continuous multimodal evidence, rather than only answering an isolated turn after a user asks. This goal does not make every user train a separate foundation model, and it does not replace the Personal Runtime with an opaque end-to-end action system.

Sub-goals:

- 6.1. Define the interaction-model boundary: a future model may synthesize time-aware candidates from vision, audio, text, screen, and structured device evidence, but `Personal Runtime` remains authoritative for Presence, privacy, permissions, routing, action validation, result correlation, audit, and feedback
- 6.2. Define the personalization boundary between a general interaction prior, user-owned Runtime memory and Presence policy, and optional user-controlled local adaptation; personal context must not be assumed to train a distinct foundation model for every user
- 6.3. Define a user-sovereign interaction-trace lifecycle: local capture and replay, reviewable minimization/redaction, explicit contribution consent, provenance and license records, retention, export, and the limitations of withdrawal after model training
- 6.4. Define the future open research path for a general interaction prior, combining legally usable public data, synthetic or simulated scenarios, and explicitly consented real interaction traces without default raw audio, video, or screen telemetry collection
- 6.5. Derive future concrete implementation milestones only after the privacy, data-governance, model-boundary, and evaluation contracts are specific enough to accept independently

Acceptance criteria:

- A written design defines the target model as an interaction-candidate generator, with Runtime-owned governance and action execution remaining outside the model boundary
- The project distinguishes a general multimodal interaction prior from per-user Runtime context, policy, memory, and any optional local adaptation
- The data-governance design makes local ownership the default and requires user review, explicit consent, minimization/redaction, provenance, and licensing before a trace can enter a shared corpus
- The design identifies that learned `silence`, timing, modality choice, and intervention intensity require time-aligned outcomes and negative or counterfactual evidence, not only generic multimodal question-answer data
- The first implementation milestone, if any, is scoped separately and does not alter the current execution route until its privacy, lifecycle, and evaluation acceptance criteria are approved

Status:

- Future / not started. This is a strategic Goal rather than a scheduled milestone; it does not change the active `M17.8` through `M23` execution route.

## Historical Reference Links

Detailed early Android and multi-edge preparation notes are preserved in the [M17 preparation archive](/root/openhalo/docs/history/m17-preparation-notes.md). The active M17 route and current acceptance boundaries remain in `Current Snapshot` and `Current Project Progress`.

## Completed Progress Index

The detailed acceptance history is preserved in [the completed-progress archive](/root/openhalo/docs/history/project-completed-progress.md). This section keeps only the current baseline needed for planning.

Accepted implementation baseline:

- `M17.0` through `M17.7.2`: public Edge API, registration, Android Edge, mobile hardening, screen/context observation, multi-edge lineage, and mobile liveness.
- `M18.1`: observation-to-snapshot decision-space integration.
- `M4.1`: Runtime-managed Host Edge lifecycle.
- `M19`: bounded SQLite state, retention, diagnostics rotation, and owner-runtime storage acceptance.
- `M20`: Hermes Agent Harness and governed action loop.
- `M20.2`: Runtime-owned interaction progress lifecycle and Android progress presentation.
- `M20.3`: stable independent Terminal Edge and user-facing receipt/progress surface.
- Hosted Coding Bridge adapter ([#14](https://github.com/nwomn/openhalo/issues/14)): managed Codex App Server lifecycle, bounded coding attention/steering, local approvals, and existing P-256/session preservation; accepted 2026-08-04.

The detailed archive is historical evidence, not a second current-status source. Update the current summary and route here when a milestone changes status.

## Research Backlog

The low-frequency design questions previously embedded here are preserved in the [research backlog](/root/openhalo/docs/research/project-open-questions.md). Load that document when doing research or making a related architecture decision; it is not required for ordinary milestone implementation.

## Current Project Progress

The project is in the post-M16 multi-edge and productization expansion phase.

### Current execution state

- `M18` Persistent Personal Runtime refactor is accepted. It introduced independent ContextFact, ContextEnvelope, Main-session, Scheduler, and attention-validation modules with unit-level contracts; Gateway remains only ingress validation, Observation persistence, ContextFact materialization, and handoff. The Main-session manager creates the native Hermes session before persisting its Runtime-owned identity, injects the bound native `SessionDB` and restored history into normal Hermes semantic calls, restores that session after SQLite-backed Runtime restart when available, and otherwise creates a new audited generation. Real MaixCAM `person_presence` facts reached the versioned ContextEnvelope and the phone through a governed Main Hermes response without media upload. The separate real Coding workflow/concurrent-child acceptance remains tracked by [#13](https://github.com/nwomn/openhalo/issues/13) and does not block M18.
- 2026-08-23 M18 foundation release: `v0.1.23` publishes the ContextFact/Envelope, SQLite v2, scheduler, Main-session, attention, Edge evidence-read, and architecture-diagram slice at commit `fa78ea3`. Its release manifest retains `sqlite-v1` as the updater compatibility level so existing installed clients can discover and stage the release; the candidate Runtime upgrades its database internally to SQLite v2 on first start.
- 2026-08-23 persistent Main-session correction: `v0.1.24` at commit `cb5ccf2` wired the Runtime-owned ID into ordinary semantic calls, but owner acceptance exposed that it did not create a Hermes `SessionDB` row or replay prior history; every restart correctly recorded `native_session_unavailable` but could not retain the conversation. `v0.1.25` begins at fix commit `4036aa8`: Gateway delegates native Main-session creation to Hermes before persisting the ID, while the Hermes adapter opens the sealed native database explicitly, passes it to `AIAgent`, and restores its conversation history on the next Main turn. Offline native-storage and Harness-contract regressions cover creation, recovery, and history injection; full regression passes 784 tests with 4 intentional skips. Immutable `v0.1.25` Release assets were published at commit `928b4d5`, and the owner Runtime updated from `v0.1.24` through `openhalo update`, then reported `v0.1.25` running and up to date. Live owner acceptance then created `openhalo-main-g6-dbe35837beb94f13b643f16306a4d6ab` at generation 6 with 6 native Hermes messages; after restart, the same ID and generation remained and its native message count advanced to 12. The recovered native transcript contains the correct later response `你的暗号是“北斗-418”。`, accepting native Main-session identity, transcript persistence/reuse, and semantic recall.
- `M17.11` has begun a bounded controller/capture bench bring-up while `M17.8` remains the current milestone; `M17.9` and full `M17.10` acceptance remain later Device Edge work. In parallel, M17.10 has a bounded in-progress physical Camera Edge Feature-validation slice using the received Sipeed MaixCAM standard kit with a bounded read-only diagnostic display; this does not advance M17.10 ahead of the route or accept it. M17.10 remains unaccepted until its separately documented Observation, governance, evidence, privacy, and human-acceptance criteria are met.
- 2026-08-30 MaixCAM Camera Edge package recovery: `maixtool` regenerated `openhalo_camera_edge_v0.1.3.zip` from the repository `app.yaml` and installed it through the device's `app_store_cli` after the system-image reflash. The Launcher now lists `openhalo_camera_edge` with the OpenHalo icon and packaged `main.py`, health, OpenSSL session, and shared visual Feature modules. Runtime configuration and the newly paired P-256 identity remain outside the package under the device-private directory. A manual Launcher start retained `python3 /maixapp/apps/openhalo_camera_edge/main.py`, refreshed health facts at `2026-08-30T13:27:59Z`, and emitted a real `present` followed by `left -> absent` person-presence transition through Runtime. The running App later persisted `present`, count `1`, allowlisted `person: 1`, and an `absent -> present` `entered` transition with roughly `0.91` confidence. The owner then asked the paired phone about the current scene; its Runtime-backed response reported a person-shaped presence but did not claim the person was the owner. This accepts the post-reflash manual App launch and camera-to-phone generic-person context path; identity is intentionally outside the current Feature, and boot auto-start, reboot recovery, long-duration supervision, and the remaining M17.10 acceptance scope stay open.
- The generic `registration registry -> ContextFact -> ContextEnvelope -> Main Hermes` implementation is the accepted M18 Runtime baseline, not M17.10 scope. M17.10 supplies real Camera Edge facts and later hardware acceptance input; it must not absorb Main Hermes implementation scope merely because the MaixCAM exposes the current gap.
- 2026-08-21 `M17.11` first physical-validation reference: use open-source ESP-KVM on an ESP32-P4 plus TC358743 HDMI-to-CSI bridge as the first Proxy Interaction Edge hardware candidate after `M17.8`. It provides a bounded HDMI/video, USB HID, virtual-media, and optional isolated ATX path for an unmodified target; OpenHalo must adapt it through its own paired Edge identity and public Edge API. This is a validation reference only, not a vendor, board, or protocol lock-in for the hardware-independent M17.11 contract.
- 2026-08-21 / 2026-08-24 `M17.11` procurement note: the first controller and capture hardware have arrived: Waveshare `ESP32-P4-WIFI6` (SKU 32020), with an onboard ESP32-C6 for the intended wireless deployment, paired with the Geekworm C790 capture bridge and required cables/power accessories. This board is not covered by ESP-KVM's current prebuilt `p4-eth` or `funcev` images, so bench assembly starts with a dedicated board profile and Wi-Fi/USB/capture pin validation; physical receipt and cabling alone are not acceptance evidence.
- 2026-08-21 `M17.11` first handset compatibility profile: use the owner's Redmi K70 Ultra as the no-native-video-output phone target. Its route is `phone wireless display -> Miracast HDMI receiver -> C790` for observation and `ESP32-P4 USB device/HID -> phone USB OTG` for input; a generic USB-C-to-HDMI dock is not part of this phone-specific baseline. This remains a later compatibility-path experiment: acceptance must separately record casting setup/recovery, end-to-end latency, keyboard/mouse coverage, secure-screen and gesture limitations, and whether the phone can maintain OTG while charging.
- 2026-08-24 `M17.11` mobile-dock bench evidence: the owner's Xiaomi Pad 6S Pro simultaneously passed powered-dock PD fast charging, HDMI display output, and USB peripheral input, validating the dock/cable path for the first full wired mobile-target integration. The same dock produced no HDMI signal from the Redmi K70 Ultra, confirming that K70 wired observation must not be used to judge dock health and remains on the wireless-display compatibility path. This is pre-integration evidence only; C790 capture, ESP32-P4 HID injection, Wi-Fi serving, recovery, and governed OpenHalo pairing remain unaccepted.
- 2026-08-28 `M17.11` target-connector direction: the intended Proxy Interaction Edge product surface exposes one full-function USB-C male target lead for both observation and control. Targets with native USB-C DisplayPort Alt Mode plus USB host support connect directly. Legacy computers use an active host-side aggregation accessory that accepts GPU HDMI or DisplayPort plus USB-A or data-capable USB-C and exposes a full-function USB-C female receptacle; it must carry video toward the Edge and USB HID back to the target with correct CC/data-role/power behavior, so a passive gender adapter or video-only USB-C output is insufficient. The current split HDMI plus four-pin USB bench wiring remains the universal validation baseline until a specific aggregation accessory passes real video, HID, hot-plug, recovery, and independent-power acceptance. This is a product connector profile, not a lock-in of the hardware-independent M17.11 Edge contract.
- 2026-08-29 `M17.11` compact-board planning decision: the first custom-board schematic baseline remains the physically validated `ESP32-P4 + ESP32-C6 + TC358743` capture path, but replaces the external dock and C790 with one target-facing full-function USB-C front end: `AP43781` PD/DP-Alt-mode controller, `PI3USB31531` crossbar, `PI3DPX1207C` re-driver, and `PS176` DP-to-HDMI converter, followed by direct HDMI-to-TC358743 CSI routing. The public Diodes AP43781 C-Link Monitor EVB is the PD/Alt-mode/layout reference; its DP output is adapted internally to PS176 rather than copied as a complete monitor board. Rev A is an unaccepted engineering prototype, targets 720p60 first, retains an independent power-input path and P4/C6 download/diagnostic points, and must explicitly prove cable orientation, PD/DP negotiation, USB HID, HDMI lock, CSI capture, network stability, thermal behavior, recovery, and governed Runtime roundtrip before it can replace the split bench. ESP32-C6 supplies only 2.4 GHz Wi-Fi, so this P4/C6 prototype does not satisfy any future dual-band product requirement.
- 2026-08-24 `M17.11` controller bring-up evidence: Windows enumerated the received Waveshare board through its CH343 USB-UART bridge as healthy `COM3`; boot logs and ROM access identified ESP32-P4 ECO2/rev 1.3, 32 MB PSRAM, and 32 MB physical flash. Before mutation, the full factory flash was read twice, sample-compared, and retained as `factory-full-32mb.bin` with SHA-256 `1b4cfc24426ad0f3e630e82d3ed1874ac4bfb6ba565f408e8a45cbde0fe80b20`. A dedicated ESP-KVM v0.33.1 Waveshare Wi-Fi-only board target was then implemented and built with ESP-IDF 6.1-rc1; the P4 image (`espkvm.bin`, SHA-256 `711b6fed946e3523598d93a6ba599d5262cc8223bae7ab808350f26fa1ae6cb8`) was flashed with per-region verification while the C6 firmware remained unchanged. Real boot acceptance covers P4/C6 SDIO streaming, Wi-Fi setup AP and DHCP, initial HTTP console access plus authentication enforcement, TinyUSB HID initialization, and the application reaching `ready`; the factory C6 reports host `3.0.6` versus co-processor `0.0.0`, so sustained networking must be accepted before any C6 replacement decision. Capture diagnostics were corrected to perform a real TC358743 address probe and to propagate failed identity/status reads rather than logging false register values. After reseating the powered link, the C790 responds at address `0x0f`, accepts the built-in EDID, reports `CHIPID=0x0000`, and exposes HDMI DDC 5 V; this accepts FPC power plus TC358743 control-plane communication. The first HDMI attempt locked the bridge but produced CSI DMA timeouts because the FPC was connected to the ESP32-P4 DSI connector. After moving the same FPC to the CSI/CAMERA connector, the bridge reached `SYS_STATUS=0xdf`, identified a `1920x1080p` input, started `1920x1080` MJPEG capture, and remained free of CSI frame-wait timeouts during the serial observation window. Selecting the `720p` EDID then made the tablet renegotiate to `1280x720p`; capture restarted automatically and remained free of CSI frame-wait timeouts, avoiding the `1080p30` mirror mode that reduced the tablet's internal refresh rate. This accepts HDMI lock and the corrected capture initialization path; browser-visible frame delivery still requires a console viewing check. After manual board reset and a separate Windows WLAN reconnect, the AP continued to receive association events and issue `192.168.4.2` by DHCP, but the Windows client then lost ARP/HTTP reachability; the local Verge proxy/TUN and the co-processor version mismatch are still competing explanations, so sustained Wi-Fi console access remains unaccepted pending a second-client check. No `usb_hid: target attached` event was observed, so four-pin USB enumeration/input, recovery, and governed OpenHalo pairing remain unaccepted.
- 2026-08-24 `M17.11` Wi-Fi isolation follow-up: the Redmi K70 Ultra alone connected to `ESP-KVM-6f46` and reached the console; after the phone disconnected, Windows alone obtained `192.168.4.2` and a direct no-proxy request to `/api/v1/auth/session` returned live `HTTP 200` authentication state. This accepts single-client AP, DHCP, P4/C6 data-path, and HTTP console reachability from both tested controller classes. When both clients were attached, Windows retained association but lost ARP/HTTP reachability even though the firmware requests four AP clients; simultaneous multi-client access therefore remains unaccepted pending co-processor/address-conflict isolation.
- 2026-08-24 `M17.11` browser video follow-up: the Windows console displayed the Xiaomi Pad 6S Pro's live `1280x720p` MJPEG capture over the AP, accepting browser-visible frame delivery for the single-client bench path. The browser held the expected direct connections for page, control, and video traffic, while the client implementation drops JPEG frames that arrive during decode and draws each completed bitmap immediately rather than building a playback queue. Perceptible delay remains unquantified, so capture-to-browser latency, fresh-frame REST latency, and full `observe -> decide -> HID act -> observe` reaction time remain explicit acceptance gaps rather than being attributed to the UI alone.
- 2026-08-24 `M17.11` first OpenHalo software slice: the hardware-independent [Proxy Interaction Edge baseline](docs/design/2026-08-24-proxy-interaction-edge-baseline.md) now composes its own Edge identity over the normal `Edge Session Link -> Gateway -> Personal Runtime` contract, with an explicit attached `target_id`/`surface_id`, structural attachment and body-free screen-frame observations, six-facet capability availability, optional native/proxy provenance binding, and exact device/target/surface validation before governed keyboard or pointer execution. The first ESP-KVM REST adapter maps capture, USB probe, and `agent_api` state, retains JPEG bodies only in a bounded Edge-local store, maps normalized pointer coordinates to adapter HID coordinates, and keeps credentials local. Disabled agent access is unavailable, while an enumeration trace is degraded rather than falsely accepted as live HID. Eleven focused tests pass. This accepts the initial contract/adapter unit slice only; public-Gateway roundtrip, Presence/audit evidence, real four-pin HID, fresh-frame understanding, action-bound post-observation, recovery, revocation, and latency acceptance remain open.
- 2026-08-29 `M17.11` persistent Proxy Edge software slice: `ProxyEdgeDaemon` now runs the proxy as a long-lived P-256 Edge session on top of the normal `Edge Session Link -> Gateway` boundary. A first run may use a one-time pairing code; later reconnects use the persisted identity. The daemon re-probes ESP-KVM before each capability/attachment publication, withdraws HID actions on an adapter failure, preserves exact action-result correlation, and emits a body-free post-action screen Observation bound to `action_request_id`; JPEG bytes remain in the local bounded cache. The focused proxy contract/daemon suite passes 14 tests. This is implementation and mocked-protocol verification only: real public-Gateway pairing, Presence/audit, live recovery/revocation, latency, and selected-firmware acceptance are still required. ESP-KVM remains a replaceable bring-up base until those accepted OpenHalo behaviors determine which drivers/recovery code deserve retention in a trimmed or replacement firmware.
- 2026-08-29 `M17.11` deployment-boundary correction: the Python `ProxyEdgeDaemon` is only a development-host harness for the public session/adapter contract; it must not be operated manually on a general-purpose computer in normal use. The deployed Proxy Interaction Edge is a native P4 firmware service that auto-starts on power-up, retains its own P-256 identity and completed provisioning in board-local storage, reconnects after network recovery, and uses only a one-time local onboarding flow (for example, a phone companion or local setup service) before unattended boots. Implementing and physically accepting that board-resident service is now the next firmware slice; no full `M17.11` acceptance is claimed.
- 2026-08-29 `M17.11` native-firmware compile slice: the Waveshare ESP-KVM ESP-IDF tree now has a thin `openhalo_edge` component and one FreeRTOS task wired after the existing P4 capture/HID initialization. It uses the explicit `UNPROVISIONED -> WAIT_READY -> CONNECTING -> ONLINE -> BACKOFF` lifecycle, existing schema-driven local settings/NVS onboarding, a board-local P-256 identity, the explicitly selected `ws://` owner transport or verified `wss://` plus configured CA, direct HDMI-frame/HID C APIs, bounded screen metadata only, and a WebSocket-only exponential reconnect that leaves P4 capture, USB HID, and C6 network ownership untouched. The pairing code is marked secret and cleared only after `connect_ok`; later boots use retained identity and configuration. The task refuses setup-AP/association-only readiness and waits for a routable uplink, HDMI/frame availability, HID enumeration, then actual WebSocket authentication. After GitHub HTTPS recovered, the declared `third_party/microlink` submodule was restored at its lock revision and ESP-IDF 6.1-rc1 cleanly built the actual Waveshare P4-WIFI6 profile with `CONFIG_KVM_WIFI=y`, retaining `esp_hosted`, `esp_wifi_remote`, and the added managed `esp_websocket_client` 1.8.0 dependency. The resulting `espkvm.bin` is 1,804,432 bytes (SHA-256 `f38a564e5d131693c86355ce496dc5d6273e68c89bb80d0a3cc95fd93d7294be`); the smallest 4 MiB app partition has 57% free. Flash, real public-Gateway pairing/action/reconnect, and board-local key-at-rest review remain required; no `M17.11` acceptance is claimed.
- 2026-08-29 `M17.11` native-firmware first boot evidence: the verified P4-WIFI6 image was written to the received Waveshare board at `COM3` (ESP32-P4 rev 1.3, MAC suffix `6f:46`) with esptool write verification. After reboot, the real board loaded the new application from `ota_0`, detected 32 MB flash and PSRAM, brought the ESP32-C6 up over SDIO with matching host/co-processor firmware `3.0.6`, served its AP/DHCP path, initialized TinyUSB HID, locked the C790 HDMI input at `1280x720`, and started capture. The board then logged `openhalo_edge: board-resident Edge task started`, proving that the task starts after capture/HID initialization without replacing the C6 path. Read-only local settings inspection confirms the expected pre-provisioning state: `oh_enable=false` and no Runtime endpoint or CA are configured; it is therefore not valid to claim a WSS failure, pairing, action, or reconnect result yet. The next bounded acceptance input is a real routable STA uplink plus public Runtime WSS endpoint, issuing CA, and one-time pairing code; no secret is stored in project evidence.
- 2026-08-29 `M17.11` STA management-path evidence: the board was configured for Wi-Fi station mode on SSID `106`; boot logs recorded the DHCP certificate address `192.168.2.106` and retained the `ESP-KVM-6f46` rescue hotspot. The controller host directly reached the board at `https://192.168.2.106` and received its local session response, so the owner does not need router-DHCP visibility to continue. This accepts local STA addressing and HTTPS management reachability only; it does not prove external DNS/TLS to the Runtime, pairing, or reconnect. OpenHalo remains explicitly disabled and unprovisioned.
- 2026-08-29 `M17.11` public-Runtime configuration and readiness evidence: live server inspection confirms the owner Runtime is `openhalo 0.1.25` listening on `0.0.0.0:8765` at canonical audience `ws://8.153.37.167:8765`. The native component was corrected from an invalid WSS-only policy to accept the project-approved owner `ws://` transport without a CA while still requiring a CA for `wss://`; the corrected image was rebuilt and flashed. The board holds the bounded bench identity `proxy-edge-1` / `tablet-1` / `main` / `tablet` and the canonical Runtime endpoint. A newly issued one-time pairing code was supplied only through the board's write-only local setting and is absent from project evidence. Reboot logs show `UNPROVISIONED -> WAIT_READY`; C6, STA, and HDMI capture are up, but the target-facing TinyUSB device has not enumerated, so the task correctly remains before `CONNECTING` and the Runtime contains no `proxy-edge-1` record. A fresh short-lived pairing code must be issued only when that physical USB link is ready.
- 2026-08-29 `M17.11` live readiness correction: later real-board inspection showed the Android target had in fact enumerated (`usb_hid: target attached`) and the C790 capture was live, while the task still remained in `WAIT_READY`. The cause is a source-ordering defect: `video_frame_store_ready()` cannot become true until the Edge registers as a local frame viewer, but the prior code registered that viewer only after `ONLINE`. The component now registers the bounded evidence viewer before waiting for readiness and releases it again when provisioning is disabled, preserving capture/HID/C6 ownership. The corrected P4-WIFI6 image built successfully (SHA-256 `f7d3bb99d36ad5f602f47a5b4deb64ac89833a55867d95e4de0fdbcba330f7cd`), but its first flash attempt found the CH343 `COM3` device absent from Windows enumeration; it must be reconnected before physical pairing evidence can continue. No `proxy-edge-1` pairing, `connect_ok`, action, or reconnect acceptance is claimed.
- 2026-08-29 `M17.11` target correction and post-flash evidence: the owner clarified that this bench target is a computer, not Android. The prior `usbprobe` `os:"android"` value was only ESP-KVM's documented best-effort USB request-pattern fingerprint and must not override owner target identity. The native source-ordering correction was subsequently written with flash verification after `COM3` returned; board configuration now names `desktop-1` / `desktop`. On the real board, the Edge evidence viewer is active and C790 MJPEG now encodes at 20 fps, accepting the readiness-deadlock repair through the local frame-store condition. The P4 reset cleared the target USB enumeration (`usbprobe` returned empty/unknown), so the task remains correctly in `WAIT_READY`; replugging the 4PIN OTG-to-computer data path is required before issuing a new code and testing Runtime pairing. No Runtime record or pairing acceptance is claimed.
- 2026-08-29 `M17.11` USB replug follow-up: the replug produced a live USB request trace and `usb_hid: target attached`, but did not complete the TinyUSB host configuration required by `usb_hid_ready()`/`tud_mounted()`. The board otherwise has Wi-Fi STA, HDMI signal, and an active local evidence viewer (MJPEG frames at about 16.5 fps), yet still correctly remains before Runtime connection and the Runtime has no `proxy-edge-1` record. This is partial electrical/descriptor enumeration, not accepted HID control; the target computer must expose a data-capable host port and finish HID configuration before pairing can start.
- 2026-08-29 `M17.11` computer HID and Wi-Fi-ready gate correction: after a controlled disconnect/reconnect, Windows cleanly enumerated the P4 composite `VID_303A&PID_400E` as eight healthy interfaces, including one HID keyboard and two HID mouse interfaces; the C790 remained locked at `1280x720` with the Edge evidence viewer encoding roughly 20 fps. The native task nevertheless remained in `WAIT_READY`. Source inspection proved that `kvm_net_has_routable_ip()` had been placed inside the Ethernet-only compile branch, so the actual C6 Wi-Fi build linked an unconditional `false` stub. The shared readiness helper was corrected so Wi-Fi STA IP state is used on P4-WIFI6 and the revised application builds with 57% partition headroom. Flash and public-Runtime pairing remain pending because the P4's CH343 programming port `COM3` is not presently enumerated while the target-facing HID connection is active; no pairing, action, or recovery acceptance is claimed.
- 2026-08-29 `M17.11` native P4 public-Runtime pairing acceptance: with the CH343 debug connector and the target-facing four-pin USB simultaneously attached, the corrected image was write-verified on the P4. A first real `CONNECTING` attempt exposed and corrected the managed WebSocket client's 4 KiB task-stack overflow; the client now owns an 8 KiB task while capture, HID, and C6 remain outside its reconnect scope. The first pre-pair retries were attributable to the expired short-lived code, not readiness or hardware: Windows kept all eight `VID_303A&PID_400E` keyboard/mouse/composite interfaces healthy, C790 capture stayed live, and the P4 reached `WAIT_READY -> CONNECTING`. A newly issued 10-minute code was written only to the board's write-only secret setting; the board then completed `CONNECTING -> ONLINE` and retained that state through the settle window. The public owner Runtime lists `proxy-edge-1` as a non-revoked `proxy-interaction` device at `ws://8.153.37.167:8765`, paired and last-authenticated at `2026-08-29T12:22:10Z`, with a distinct P-256 fingerprint. This accepts first real board-resident automatic service readiness, public WebSocket pairing/authentication, and Runtime device registration; governed Runtime-to-P4 keyboard/pointer action receipt, post-action Observation receipt, code-cleared restart reconnect, and network-loss recovery remain unaccepted.
- 2026-08-29 `M17.11` visual-control architecture correction: owner review established that body-free frame hashes and dimensions are useful only as availability/health evidence; they cannot support Runtime decisions about what an unmodified target displays or safe screen-dependent control. A raw-frame request before every input is also not the chosen design. Proxy Interaction Edge now adopts the Camera Edge model: Runtime confirms a target/surface Screen Profile and sends only allowlisted, versioned Feature subscriptions; P4 emits compact capture-health, freshness/change, and action-correlated structured Observations while holding a small local policy-bounded frame buffer; a candidate change, user inspection, or policy-approved verification causes an authenticated bounded evidence query; scoped Runtime vision returns expiring structured Understanding for Action Layer admission. Raw video remains off and raw stills stay outside ordinary durable context by default. P4 must report `unknown`/`evidence_needed`, rather than inventing GUI semantics it cannot locally determine. Runtime must not issue a screen-dependent input action blind when the active Profile lacks the required Understanding or evidence. The existing metadata-only native registration remains a completed transport/identity slice, but is insufficient for visual-control acceptance; Feature/Profile subscription, local buffer/change Features, evidence request/transfer, scoped vision understanding, governed action receipt/verification, restart reconnect, and network-loss recovery are now the active firmware/Runtime completion scope.
- 2026-08-29 `M17.11` governed Proxy Screen protocol slice: the shared Proxy Edge/Runtime implementation now registers `proxy.screen.features`, `proxy.screen.profile.configure`, and capture-available `proxy.screen.evidence.read`. A Runtime-confirmed, expiring target/surface Profile allowlists only the initial capture-health, digest-based change, and action-effect Features; the Edge retains a four-item bounded local evidence index and sends no Feature frame until a Profile is active. `proxy.screen.evidence.read` emits a body-free action result first, then a byte-limited correlated `evidence_transfer`; Gateway accepts it only after that result, calls an injected transient vision evaluator, writes only safe hash/size/reference/state audit metadata, and returns an expiring `understanding_update`. A Profile requiring understanding causes Proxy keyboard/pointer execution to fail closed without the current matching authorization. Eighteen focused Proxy tests cover profile allowlisting, blind-HID rejection, bounded transfer integrity, Runtime authorization, non-persistence of JPEG bytes, and rejection of unapproved transfer; compile validation passes. This is host/runtime protocol acceptance only: P4 native Feature sampling/ring transfer, a configured production vision provider, public Runtime session/dispatch, visual result verification, restart reconnect, and network-loss recovery remain unaccepted.
- 2026-08-30 `M17.11` Profile architecture scope correction: the existing hard Profile activation behavior remains an implemented protocol experiment, but is not the future visual/audio/microphone ingestion architecture. Attention Profiles will be a validated overlay over safe base Observations rather than a second compact/filtering stage. Runtime Profile delivery, lease control, and automatic dispatch are deferred; they do not block the now-prioritized visual plus audio/microphone perception-and-control closed loop.
- 2026-08-30 `M17.11` base Observation migration slice: the Proxy harness now registers and publishes `proxy.screen.base_observe` without a Profile, carrying only capture-health, digest-based change, and action-correlated change with target/surface binding and Edge-local evidence references. Gateway schema validation accepts these registered facts into ContextFact; a result-authorized `proxy.screen.evidence.read` can resolve a retained base reference and return a short-lived Understanding without retaining JPEG bytes. The old Profile-gated feature protocol remains an isolated experiment. Focused Proxy tests cover Profile-free ContextFact ingress, bounded evidence/Understanding, existing Profile behavior, and daemon publication. This is host/runtime contract validation only; P4-native base sampling/transfer, public Runtime end-to-end dispatch, visual-action verification, restart reconnect, and network-loss recovery remain unaccepted.
- 2026-08-30 `M17.11` P4 base-Observation deployment and ingress evidence: the external Waveshare P4 bring-up component separately advertises `proxy.screen.base_observe`, sends capture-health and bounded digest-based change on ONLINE entry and every five seconds, emits action-correlated change after an action request, and permits a result-authorized evidence read without an active Profile; the Profile-gated feature path remains intact. The ESP-IDF 6.1 Waveshare build produced a 1,812,416-byte application with 57% app-partition free, was write-verified and booted on the physical P4, and preserved the board-local Runtime endpoint, P-256 provisioning, and `proxy-edge-1`/`desktop-1`/`main` binding. A UART control-line mistake invoked the documented physical recovery path, which disabled `oh_enable` and switched to the local hotspot; the preserved settings were then restored through that local recovery console without re-pairing. On the owner public Runtime, the recovered board registered `proxy.screen.base_observe` and persisted `proxy.screen.capture_health.v1` (`ready`, `1280x720`) plus `proxy.screen.change.v1` as structural ContextFacts; a 12-second server-side sample observed the periodic health record count rise from 37 to 39. A first deployed timestamp path exposed the P4's uninitialized 1970 clock, so the component now starts SNTP after the routable network readiness gate and holds time-dependent observations until the clock is plausible; the corrected image was rebuilt, write-verified, and reflashed. Runtime then received advancing real-time health facts (for example `2026-08-30T04:18:01Z`) with future 120-second expiry, accepting fresh P4 base-Observation delivery and generic Gateway-to-ContextFact ingress. The earlier change fact remains stale because the visible target content did not change during the corrected run; a deliberately induced visual change still needs its own acceptance. Visual semantics, bounded JPEG/Understanding dispatch, authorized HID/action verification, restart reconnect, and network-loss recovery remain unaccepted.
- 2026-08-30 `M17.11` screen-read contract correction: owner review supersedes the special `proxy.screen.evidence.read`/`evidence_transfer` path for normal screen reads. The product contract is now the ordinary action path: Hermes proposes registered `proxy.screen.read` with target/surface, `freshness:"latest"`, and a max-byte bound; P4 returns the current JPEG once in the correlated `action_result.payload`; Gateway performs generic MIME/size/SHA-256 validation, removes the encoded bytes before state persistence, invokes transient vision, and gives only its bounded text Understanding to the continuing Hermes turn. P4 capability metadata now publishes this input schema/affordance and the Runtime projects it into Hermes grounding. Profile remains an isolated Feature/HID experiment, not a prerequisite for reading a screen. The focused Python suite passes 73 tests and the native Waveshare image builds with 57% app-partition free. The P4 image was flash-verified on the Waveshare bench and reconnected automatically; the owner Runtime was atomically updated to `v0.1.27` / immutable `5511059` with the sole configured model explicitly marked vision-capable. Runtime capability announcements now replace the complete device snapshot, so this reconnected P4 advertises `proxy.screen.read` and no stale `proxy.screen.evidence.read`; Runtime health remains ready. The real owner loop is accepted for this narrow screen-read slice: two Presence-allowed Hermes requests at `2026-08-30T09:22:12Z` and `09:24:15Z` reached the P4, returned current 75,331-byte and 72,566-byte JPEG attachments as normal action results, produced distinct transient visual Understandings, retained no encoded JPEG in Runtime state, and each triggered a successful `notification.show` result on the Android Edge. The deployed `v0.1.28` Runtime additionally requests high visual detail for approved screen reads; small-text/OCR behavior requires a fresh owner check. HID action verification, restart reconnect under an active action, and network-loss recovery remain unaccepted.
- 2026-08-30 M17.11 physical HDMI-source compatibility update: owner bench verification connected the same P4 capture path to a tablet and confirmed both real image capture and Runtime-mediated content reading. This accepts source compatibility for the tested computer and tablet inputs; it does not by itself alter the logical target binding or accept tablet HID control.
- 2026-08-31 `M17.11` legacy-desktop single-cable aggregation evidence: the received AV Access `H2C01` active adapter was installed as `RTX 4060 HDMI + desktop USB-A -> H2C01 -> full-function USB-C -> existing dock -> C790/P4`. Windows simultaneously retained the healthy P4 composite keyboard/mouse interfaces (`VID_303A&PID_400E`). More importantly, at `2026-08-31T10:13:45Z` the public Runtime recorded a successful governed `proxy.screen.read` from `proxy-edge-1` / `desktop-1` / `main`: the board returned one 79,987-byte `image/jpeg` attachment, whose persisted record contains only its evidence ID, MIME type, size, SHA-256, and transient visual Understanding (PotPlayer video frame with the subtitle “He was sympathetic.”), not encoded pixels. This accepts the tested H2C01 route for HDMI observation into the board and USB-HID enumeration back to the legacy desktop through one target-facing USB-C lead. It does not yet accept Runtime-to-HID action execution through this accessory, hot-plug/recovery, independent-power behavior, or a final product BOM/cost decision.
- 2026-08-30 `M17.11` native governed-screen implementation slice: the P4 `openhalo_edge` component now advertises the Profile/Feature/Evidence capabilities, stores up to four independently copied JPEG evidence frames in a 96 KiB-per-slot PSRAM-first ring, emits only Profile-selected capture-health/change/action-effect observations, and transfers a named retained frame only after a correlated evidence action result. It never treats the live encoder buffer as durable evidence. Profile and Runtime Understanding use a bounded monotonic lease in addition to the audit RFC3339 expiry, so `require_understanding` HID is fail-closed even before a board wall clock is trusted; an expired/missing/mismatched authorization is rejected without resetting capture, HID, or C6. The ESP-IDF 6.1 Waveshare P4-WIFI6 build completes with the component. This is compile-only native evidence: flash, public Runtime Profile dispatch, bounded JPEG transfer/vision return, authorized HID, restart reconnect, and network-loss recovery still require physical acceptance.
- 2026-08-30 `M17.11` native boot and Runtime-session evidence: a first P4 image exposed an unrelated legacy HTTPS listener/lwIP assertion before `openhalo_edge_init`; the normal provisioned Edge boot therefore keeps that legacy browser listener dormant, while the existing physical reset gesture disables `oh_enable` and restores the local-console recovery path. The actual Waveshare P4-WIFI6 configuration was then rebuilt in its own build directory with C6 `esp_hosted`/`esp_wifi_remote`, flashed with hash verification, and booted on the physical P4. Serial evidence shows C6 SDIO streaming, matching 3.0.6 host/co-processor firmware, Wi-Fi STA DHCP `192.168.2.106`, computer USB HID attach, C790 `1280x720` MJPEG frame store, followed by `WAIT_READY -> CONNECTING -> ONLINE` against the owner Runtime. A 22-second post-connect window produced no disconnect/retry. This accepts native boot through the actual P4/C6/HID/capture readiness gate and automatic public Runtime reconnection for this image; Runtime Profile dispatch, bounded JPEG transfer/vision return, authorized HID action, restart reconnect, and network-loss recovery remain unaccepted.
- 2026-08-24 `M17.11` authenticated Agent API bench evidence: after the owner explicitly enabled ESP-KVM's upstream `Agent REST API`, a one-time Edge-local authenticated request to `/api/v1/video/frame.jpg` returned a fresh `1280x720` JPEG (73,632 bytes) without exposing or persisting the password. This accepts the real still-frame REST path used by the first OpenHalo adapter. The owner had just locked the Xiaomi Pad 6S Pro, but the captured HDMI frame still showed the prior/external note canvas rather than a lock screen; whether the tablet freezes its external output, keeps an external presentation active, or applies different lock semantics remains unaccepted and must not be inferred from this frame alone.
- 2026-08-24 `M17.11` password-free bench follow-up: the owner temporarily disabled ESP-KVM `Require login` and restarted the controller. After the P4/C6 network service recovered, `/api/v1/auth/session` reported `required:false`; a new unauthenticated `/api/v1/video/frame.jpg` request returned `HTTP 200 image/jpeg` with a changed `1280x720` frame (73,352 bytes). This accepts password-free Agent REST access for the isolated single-client bench only. It is not the production security baseline because every AP participant can then read the target screen and invoke enabled HID endpoints; owner deployment must restore authentication or place equivalent access control in front of the adapter.
- 2026-08-24 `M17.11` first control-path probe: video remained locked at `1280x720` MJPEG and `agent_api=true`, but `/api/v1/system/usbprobe` returned `os:"unknown", trace:""`; a bounded center-pointer move was explicitly rejected with `HTTP 409 {"error":"no USB target attached"}`. This proves the Agent API fails closed rather than silently reporting control success, but real control remains blocked below OpenHalo at the P4 target-facing USB OTG enumeration boundary. The Waveshare board's separate 4PIN USB OTG connector must reach the powered dock's downstream USB host port; the flashing/power Type-C and C790 `VIDEO` header are not substitutes.
- 2026-08-27 `M17.11` first four-pin HID acceptance slice: after a clean controller reset and explicit Windows WLAN reassociation, `/api/v1/system/usbprobe` returned `os:"android"` with a non-empty enumeration trace, replacing the earlier empty-probe/`409 no USB target attached` result for the current Xiaomi Pad 6S Pro dock route. A fresh pre-action JPEG was captured, a bounded absolute center-pointer move (`x=16384`, `y=16384`, no click or key) returned `HTTP 200 {"ok":true}`, and a fresh post-action JPEG also returned `HTTP 200`. This accepts target-side four-pin USB enumeration, one Agent REST pointer primitive, and the first transport-level `observe -> HID act -> observe` loop. It does not yet prove visible cursor displacement because the tablet's foreground content changed between frames and no pointer was visible in the post-frame. Continuous operation remains blocked: after an API request the P4/C6 data path repeatedly stops answering ICMP/HTTP while the setup AP remains visible, and Windows must disconnect/reassociate before the next request succeeds. Power stability, P4/C6 SDIO/co-processor behavior, multi-request recovery, visible input confirmation, latency, and governed OpenHalo roundtrip therefore remain unaccepted.
- 2026-08-27 `M17.11` visible-drag diagnosis and firmware candidate: an owner-authorized note-canvas drag produced two new marks first at the landscape edge and, after swapping the tablet's rotated absolute axes, two marks inside the canvas. This accepts that Android and the note application execute the P4's absolute-pointer button reports, while exposing separate orientation calibration and held-motion continuity defects. Source inspection found the immediate continuity cause in `kvm_hid`: `merge_mouse()` coalesced every queued motion with an unchanged button mask, including button-held drawing/drag reports, so the target retained only edge positions rather than the path. The Waveshare firmware candidate now preserves every held-button absolute and relative motion while retaining idle-motion coalescing; an ESP-IDF 6.1-rc1 incremental build completed and produced a 0x1b2ba0-byte `espkvm.bin` (1,780,640 bytes, SHA-256 `95e60d3a16d0f4a26464dc34e035dbca6e1b914262b96ed0f97d137395a2e79c`) with 58% of the smallest app partition free. The application partition at `0x20000` was then flashed through the board's CH343 `COM3`, and esptool's write-back hash verification passed without replacing the bootloader, partition table, settings, or C6 firmware. A clean boot reached `espkvm: ready`, started HID, restored the Wi-Fi AP/Web service, locked the C790 at `1280x720p`, and restarted capture after one transient signal loss. The candidate remains unaccepted only until the owner reconnects the four-pin target path and the same rotated-axis note-canvas drag yields a continuous line; continuous multi-request Wi-Fi stability remains a separate open blocker.
- 2026-08-28 `M17.11` visible-control acceptance follow-up: after reconnecting the Xiaomi Pad 6S Pro through the four-pin target path, the patched firmware again identified `os:"android"`. Absolute-pointer drags still collapsed against the landscape edge because Android maps that interface incompatibly with the rotated external display, so mobile control must not use the current absolute-coordinate adapter path. The independently enumerated relative-mouse interface was then used to move into the note canvas, press the primary button, preserve 24 held-motion reports, and release it. A fresh post-action Agent REST frame showed one new continuous black horizontal stroke, accepting real Android pointer movement, button hold, drag continuity, release, and the first visually proven `observe -> act -> observe` loop through `tablet -> dock -> four-pin USB -> P4 HID` on the patched firmware. This completes the bounded Xiaomi Pad 6S Pro Proxy Interaction Edge technical-feasibility validation: an unmodified tablet can be observed over HDMI capture and visibly controlled over relative USB HID through the real controller/dock path. It accepts the physical tablet feasibility slice, not full M17.11: the OpenHalo adapter still needs a governed relative-pointer action/calibration contract, the P4/C6 AP continues to require Windows WLAN reassociation between many requests, and recovery, latency, revocation, security restoration, and public Runtime roundtrip remain open.
- 2026-08-28 `M17.11` first desktop connection and visible-keyboard acceptance slice: with the existing dock attached to the owner's ASRock B550M-ITX/ac rear USB-C data port, Windows enumerated the ESP32-P4 composite device `VID_303A&PID_400E` with one HID keyboard and two HID mouse interfaces, accepting the target-side desktop USB transport and proving that the dock can return Proxy Edge HID over this computer's USB-C data path. The C790 initially reported `signal:false`, and Windows exposed only the two existing physical monitors, so the same motherboard USB-C connection did not provide a usable display source. After connecting a discrete-GPU video output through the universal split HDMI plus USB profile, Windows activated a third monitor with EDID name `ESP-KVM` while every P4 keyboard/mouse interface remained present and healthy. After controller recovery, Agent REST reported `signal:true` at `1280x720`; an owner-authorized `Win+R` HID chord returned `HTTP 200 {"ok":true}` and visibly opened the Windows Run dialog. Agent REST then typed `notepad`, submitted Enter through the type endpoint's newline mapping, and launched a new Notepad process; a second typed token changed its window title to `*OPENHALO-PC-EDGE-20260828 - Notepad`, independently confirming visible application launch and real desktop text input rather than relying on REST acknowledgements alone. This accepts simultaneous host-side desktop video enumeration and USB HID transport plus visible keyboard execution over the pure two-line profile. A controlled four-screen rerun then captured Windows `DISPLAY1`, `DISPLAY2`, and `DISPLAY3` beside every Agent REST JPEG. It established that ESP-KVM observes the `1280x720` `DISPLAY3`, while the owner-opened Notepad and a safe `OPENHALO-SAFE-1135` KVM text action were on `DISPLAY1`; their absence from the KVM frame was therefore a multi-monitor target-selection issue rather than loss of HDMI capture. The first snapshot after an idle/no-viewer interval can still expose the immediately preceding frame before the demand-started encoder catches up, while a follow-up snapshot matches the current capture surface and reports active encode rates during wake-up. Persistent capture/encoder failure is not supported by this rerun. The owner then configured Windows to duplicate the primary desktop to the ESP-KVM output; Windows collapsed the clone pair to one logical surface, and a follow-up KVM frame showed the same Notepad and `OPENHALO-SAFE-135` content as the primary display. Starting from that KVM observation, Agent REST typed a newline plus `OPENHALO-DUPLICATE-1142`; the next KVM frame visibly contained both lines and matched the Windows primary screenshot. This accepts the first desktop same-surface `observe -> keyboard act -> observe` loop without using target process state as acceptance evidence. Duplicate mode is the safe first desktop multi-monitor profile; extended-mode focus/coordinate governance remains unaccepted.
- 2026-08-28 `M17.11` desktop pointer, idle-frame, and reference-firmware boundary follow-up: from a fresh KVM frame in duplicate mode, an absolute click at adapter coordinates `x=3456,y=3095` repositioned the Notepad caret onto the first line, and the immediately following `MOUSE-` text appeared at that clicked location in the post-action KVM frame. This accepts the first visually proven desktop absolute-pointer `observe -> click -> keyboard act -> observe` loop. After a 30-second no-viewer interval, a new HID marker was already visible in the first requested JPEG, which returned in about 1.4 seconds; the second frame was consistent. A single retry remains a defensive freshness policy for display-mode changes, where an earlier run returned the immediately preceding frame once, but routine idle wake-up did not require it. A controlled `/api/v1/system/restart` then restored the ESP32-C6 AP and Windows association, while P4 HTTP availability remained intermittent: a forced WLAN disconnect/reassociate produced one `HTTP 200` response before later requests timed out again despite 99% association signal. This records the upstream reference stack's recovery defect without making it an OpenHalo product blocker. ESP-KVM is now explicitly scoped as a disposable bench/reference implementation: Proxy Interaction Edge may substantially remove its web UI, VPN, MQTT, virtual-media, and other nonessential services, reuse only selected hardware drivers, or replace the firmware entirely. The product invariant is the hardware-independent governed observation/control contract plus accepted HDMI-to-CSI and USB-HID behavior, not ESP-KVM's REST API, network topology, or source tree. Recovery acceptance therefore moves to the eventual selected Proxy Edge firmware and transport.
- 2026-08-28 `M17.11` matched-firmware recovery slice: the ESP-Hosted `3.0.6` ESP32-C6 coprocessor image was built from the official OTA coprocessor example (`1,171,616` bytes, SHA-256 `85683adf3194b46c4cd1af0676fd2690a443ba8fc268919eba0ab503bca2a318`). A recovery-only Waveshare P4 build embeds that image and checks the C6 version after the SDIO handshake, writing only on mismatch. The independent P4 image compiled successfully (`2,953,680` bytes, 30% of the smallest app partition free; SHA-256 `8c43572752bb11c94ccef57adbf9144a784fce67fc26562eac26b38b0160e1b7`) with the recovery symbol present and OTA/reset configuration enabled. After the owner connected the P4 CH343 Type-C, only the P4 application region at `0x20000` was flashed and esptool write-back verification passed; boot logs then reported `host=3.0.6 coprocessor=3.0.6 (match)`, so the recovery hook left the C6 untouched. The P4 reached `espkvm: ready`, brought up USB HID, Wi-Fi AP, and `1280x720` capture. A fresh Windows WLAN association still produced HTTP timeout/reset despite 99% signal, so matching the C6 image did not yet clear the reference stack's P4/C6 data-path stall. The factory C6 image has not been independently backed up, and stable network/API recovery remains unaccepted.
- 2026-08-28 `M17.11` 40 MHz SDIO framing A/B: the original streaming/streaming pair served five consecutive control requests (`HTTP 200`, 0.13--0.89 s) and five JPEG frame requests (`HTTP 200`, 0.63--2.30 s) at 99% RSSI. A packet-mode C6 was built and provisioned through a one-time P4 recovery image, then a P4 packet-mode host was built with the clock still fixed at 40 MHz; serial logs verified `slave=packet host=packet`, AP, HID, and 720p capture all started, but after explicit WLAN reassociation all five control and five frame requests timed out. Reverting only the P4 host to streaming while retaining the packet C6 reproduced the timeout, isolating this test failure to the experimental C6 packet transmit path rather than P4 host receive framing. The known-good C6 streaming image was then restored and the final serial handshake again reported `slave=streaming host=streaming` with SW_AGGR. This rejects packet framing as a current workaround; it is not an accepted recovery fix or Proxy Interaction Edge transport choice. The reference AP can still intermittently associate without serving HTTP, so stable network/API recovery remains unaccepted.
- 2026-08-28 `M17.11` 20 MHz streaming SDIO A/B: with the C6 left on the restored official `3.0.6` streaming image, a P4-only application image set only the P4 host SDIO clock to 20 MHz (streaming framing, AP, HID, and 720p capture unchanged; no C6 recovery payload), and esptool write-back verification passed at `0x20000`. At 99% RSSI, the first post-flash request group returned five control `HTTP 200` responses (1.33 s, 3.93 s, then 0.05--0.07 s) and five JPEG `HTTP 200` responses (0.06--0.13 s). A subsequent hardware reset, explicit Windows WLAN reassociation, and second group of three control plus three JPEG requests also all returned `HTTP 200` in 0.06--0.08 s. This makes 20 MHz streaming the current reference configuration for the Waveshare P4/C6 bench, while packet framing remains rejected. It is bounded restart/reconnect evidence, not yet long-duration recovery acceptance; the eventual Proxy Interaction Edge still owns its product transport and recovery policy.
- 2026-08-28 `M17.11` 20 MHz sustained/recovery follow-up: a 20-round immediate sequence at 99% RSSI exposed ongoing reference-stack degradation: control returned `HTTP 200` in 17/20 rounds and timed out in 3/20; JPEG returned `HTTP 200` in every round but usually took 3--4 s, versus the initial sub-130-ms frames, and several responses were materially smaller. A Windows-only disconnect/reassociate did not make the data plane immediately ready (the first three samples contained control timeouts) but recovered by samples four and five to roughly 70--115 ms. After a P4 hard reset, the first five samples all failed or timed out despite reassociation; after an additional 30-second quiet period, the first delayed sample still failed, while the final two recovered to roughly 50--70 ms, with one transitional 4.15-s JPEG. Thus 20 MHz reduces the early all-timeout failure seen at 40 MHz but does not deliver stable sustained latency or deterministic post-reset readiness. It remains a bench configuration only; product recovery must detect service readiness rather than AP association and must be implemented in the future trimmed/replaced Proxy Interaction Edge transport.
- 2026-08-28 `M17.11` GPIO54/SDIO startup audit: the generated Waveshare WIFI6 configuration resolves `CONFIG_ESP_HOSTED_HOST_RESET_GPIO=54`, active-low polarity, `CP_RESET_STRATEGY_ALWAYS`, and a 1500 ms post-reset settle. The ESP-Hosted reset routine configures GPIO54 push-pull, parks it high for 10 ms, drives it low for a nominal 10 ms reset pulse, returns it high, and waits 1500 ms; `esp_hosted_connect_to_slave()` reaches this reset-before-card-init path after the app's explicit `esp_hosted_init()` call, with the pre-`app_main` constructor disabled. The app's WiFi boot branch does not call `kvm_storage_init()` at all, and no other current callsite mounts or resumes microSD in WiFi mode, so an application-level TF-card mount race is not present. This rules out the two suspected software-order/configuration errors but cannot prove that GPIO54's electrical waveform reaches the C6 EN pin; the remaining hypotheses are board-level reset connectivity/thresholds, SDIO signal integrity or controller state, and runtime transport recovery. A scope/logic-analyzer capture of GPIO54/C6 EN and a verbose failed-boot ESP-Hosted log are the next diagnostic evidence; 20 MHz remains bench-only.
- 2026-08-28 `M17.11` 40 MHz ESP-Hosted startup trace: a P4-only diagnostic image (application partition `0x20000`, esptool read-back verified; C6 unchanged) retained the Waveshare Wi-Fi profile, GPIO54 reset, no-microSD Wi-Fi path, four-bit 40 MHz SDIO streaming, and C6 streaming firmware. A first all-global-Debug attempt was invalid as a transport test: P4 PSRAM-calibration Debug output delayed boot until `CHIP_LP_WDT_RESET` before `app_main`. The final selective-debug image used global Info plus maximum Debug and only the relevant ESP-Hosted tags. Its saved COM3 traces cover three controlled cold starts: each reset GPIO54, reported `Card init success`, negotiated host/C6 `3.0.6`, `SDIO SW_AGGR`, and `slave=streaming host=streaming`, then reached AP/Web `ready` without WDT or `esp_hosted bring-up failed`. This is a positive 40 MHz boot baseline, not a failed-boot trace; it rules out the suspected initial GPIO/pin/version/configuration path for those samples, while the separately observed sustained/reconnect data-plane stall remains unaccepted and must be reproduced to provide a failure log.
- 2026-08-28 `M17.11` 40 MHz running-stall reproduction: immediately after the successful selective-debug cold-start series, Windows associated to the visible `ESP-KVM-6f46` AP at 99% / -21 dBm and received `192.168.4.2` by DHCP, but no `192.168.4.1` neighbor/ARP entry materialized. Direct no-proxy requests to both `/api/v1/auth/session` and `/api/v1/video/frame.jpg` timed out. This reproduces the prior failure shape as a post-boot AP/DHCP versus data-plane split, not an SDIO initialisation failure. The selectively enabled P4 Hosted tags emitted no new UART line while stalled, so a maintainer-requested failed-boot trace is not the right artifact for this condition; any upstream escalation needs an ESP-Hosted/C6 running-stall trace or added runtime transport diagnostics.
- 2026-08-28 `M17.11` running-stall transport probe: a second P4-only temporary diagnostic image kept the 40 MHz streaming/C6 `3.0.6` configuration but bounded the host's SDIO interrupt wait to one second and sampled the C6 credit register on each idle interval. After normal boot and Windows association at 99%, three direct session requests plus three JPEG requests each timed out at about three seconds. During the same interval, P4 reported `state=2` (TX active), `credit_rc=0`, `credits=2`, constant `txbuf=134`, and constant `rxbytes=62202`, while no C6 IO interrupt arrived. Thus the P4 can still perform the SDIO register transaction that reads C6 credits; the reproduced failure is more narrowly a missing/stuck C6-to-P4 data-ready interrupt or Wi-Fi-frame forwarding path, not a complete SDIO command-bus loss. The bounded wait makes IDF print `sdmmc_host_io_int_wait ... failed to wait io interrupt` once per diagnostic timeout; that line is evidence of the absent interrupt under this probe, not an independent reset. C6 UART pads are documented by Waveshare but are not connected in this bench, so resolving C6 Wi-Fi/event generation versus the P4 interrupt-wait path requires a 3.3-V USB-UART capture on those labeled pads or a C6 diagnostic firmware path.
- 2026-08-28 `M17.11` upstream contribution preparation: after the ESP-KVM maintainer requested separate contributions, the independently accepted held-pointer-motion repair was cleanly transplanted onto ESP-KVM `v.0.39.0` as one commit (`d7a6ced`, one file and eight added lines) and pushed by SSH to `nwomn/espkvm:codex/held-pointer-motion`. Its pending PR text reports the real Xiaomi Pad note-canvas continuous-stroke validation and explicitly excludes board definitions, CMake, capture, and network changes. The current local ESP-IDF is `6.1`, while the upstream `v.0.39.0` lockfile requires `6.2`, so a new-mainline full build was not claimed; the isolated repair was previously built and physically accepted on the Waveshare test image. The board-support PR remains intentionally blocked on accurate reporting and resolution or maintainer acceptance of the P4/C6 Wi-Fi recovery defect.
- 2026-08-28 `M17.11` upstream held-pointer contribution: the owner submitted [ESP-KVM PR #28](https://github.com/espkvm/espkvm/pull/28), `fix: preserve held pointer motion`, from `nwomn:codex/held-pointer-motion` to `espkvm:main`. GitHub reports it open with one commit and the declared single-file scope. The PR carries the validated Xiaomi Pad 6S Pro continuous-stroke evidence but makes no claim that Waveshare P4/C6 networking is accepted.
- 2026-08-28 `M17.11` maintainer communication: after the PR was opened, a plain-text personal Gmail draft was created for the ESP-KVM maintainer using the public GitHub commit contact. It links PR #28 and reports the Waveshare P4/C6 hardware success, the 40 MHz/packet/20 MHz transport measurements, the nondeterministic post-reset service readiness, and the decision to keep board support separate until the maintainer advises on SDIO/recovery expectations. The message remains unsent for owner review; no mailbox send or credential change was performed.
- 2026-08-28 `M17.11` maintainer draft correction: the owner requested first-person wording because this is a solo project. A corrected replacement draft was created with all developer statements using `I`/`my`; the earlier unsent draft was left untouched because the safe Gmail MCP exposes creation but not in-place draft editing or deletion. No message was sent.
- 2026-08-29 `M17.11` upstream board contribution: the owner submitted [ESP-KVM PR #29](https://github.com/espkvm/espkvm/pull/29), `feat: support Waveshare ESP32-P4-WIFI6`, from `nwomn:codex/waveshare-p4-wifi6-pr` to `espkvm:main`. It is a clean source-only board definition: Wi-Fi-only defaults/CI target, physical header map and GPIO6 reservation, mDNS eligibility, and board documentation. It intentionally excludes C6 binaries, factory dumps, P4 diagnostic code, experimental SDIO modes/clocks, capture/Microlink changes, and dependency-lock changes. The same update posted the sanitized 40 MHz running-stall probe to [Issue #27](https://github.com/espkvm/espkvm/issues/27#issuecomment-5454805958), accurately distinguishing it from a failed boot: P4/C6 startup succeeds but later AP/DHCP association can lose C6-to-P4 data-ready interrupts while credit-register reads still work. The PR does not accept stable ESP-KVM networking or change the Proxy Interaction Edge product boundary; CI/maintainer review remains pending.
- 2026-08-29 `M17.11` Waveshare SDIO pull-up stability slice: a normal ESP-KVM Waveshare build now enables P4 internal pull-ups on SDIO CLK/CMD/D0--D3 before ESP-Hosted claims the bus, supplementing the board's 51k external pull-ups. On the physical P4 rev 1.3 / C6 `3.0.6` bench, the 4-bit 40 MHz streaming link repeatedly reached `Card init success` and `ready`; three P4/C6 resets followed by explicit WLAN reassociation each restored DHCP and HTTP. A further ten disconnect/reassociate cycles completed thirty `HTTP 200` page requests, and a 100-request page burst transferred about 22 MB without an error. Windows did not automatically rejoin within the first eight seconds after one P4 reboot even though the SSID remained visible; explicit reassociation succeeded, so client auto-join policy remains separate from the accepted adapter data path. This accepts a controlled single-client ESP-KVM AP/DHCP/HTTP transport baseline for the Proxy Interaction Edge bench, not public-Gateway, multi-client, long-duration, security, or full `M17.11` acceptance.
- 2026-08-29 `M17.11` upstream pull-up resolution: ESP-KVM maintainer dexif merged the minimal invocation repair as [PR #30](https://github.com/espkvm/espkvm/pull/30) and released `0.41.1`. He confirmed `0.40.1` shipped the helper and Kconfig option without calling the helper, so the prior board build could not have exercised internal SDIO pull-ups. Our physical 40 MHz stability measurements are the first reported evidence that invoking the pull-ups resolves the observed single-client running stall; upstream also added CI protection against unreachable helper code. The short Windows rejoin delay after a P4 reboot is now treated as a separate client association timing question unless it persists beyond roughly ten seconds. This resolves the ESP-KVM reference-stack pull-up defect, but does not broaden the accepted controlled bench into full M17.11 Gateway, multi-client, long-duration, or governance acceptance.
- The remaining M17 child milestones follow the accepted M18 Runtime refactor and provide Edge-specific acceptance evidence without changing its generic registered-context contract.
- `M20.1`, `M21`, `M22`, `M23`, and `M24` remain later roadmap items with planning summaries rather than active implementation tasks. `M24` is independent of `M17.10`; the latter remains only the MaixCAM v1 validation slice.
- `M19` is accepted and reopens only if bounded-storage or response-path regressions recur.

### Current architecture direction

- Frontend remains `Device Edge`; backend remains `Personal Runtime`.
- The runtime path remains `event -> compact snapshot -> grounding bundle -> prompt/context package -> model-backed proposal formation -> Presence Router -> execution planning/action`.
- 2026-08-07 continuous interaction implementation: `InteractionPool` is a sibling internal module of Proposal Formation, Presence Router, and Execution Planning inside `Agent Runtime`; it extends the former one-shot ledger into the source-neutral lifecycle manager. Persistent interactions retain bounded objectives, watches, obligations, process state, health, and Hermes child-session lineage. `RuntimeOrchestrator` dispatches ordinary Edge facts, bounded evidence, action results, timeouts, and health changes to `InteractionPool`, then reawakens the same Hermes session when another semantic turn is required. No independent `ContinuationRouter`, Coding lifecycle, or device-specific Runtime lifecycle is introduced. Edge remains responsible for high-density structured facts, coverage, and bounded evidence buffers, while Runtime owns event interpretation, evidence escalation, health reconciliation, Presence, and final completion decisions. The interaction-pool continuation, ActionBatch, and full Python regression acceptance all pass on this baseline.
- 2026-08-08 Pool-owned cross-interaction context projection: when a new user Interaction is created, `InteractionPool` now returns a bounded summary of same-source persistent/recent process records and stores only their Interaction IDs plus process-state versions in the new record lineage. `RuntimeOrchestrator` carries those summaries through the existing grounding bundle to the new Hermes turn, so status queries can use authoritative completed/failed/monitoring facts without a duplicate RuntimeState process index or full Interaction-history injection. Focused Pool and Orchestrator regressions cover the projection and reference boundary.
- 2026-08-08 projection hardening: shared-edge summaries retain causal/process/capability labels for disambiguation, Pool and grounding apply independent byte/count limits, and every Pool-owned lifecycle/process mutation advances the persisted process-state version used by lineage references.
- 2026-08-08 SQLite recovery/update hardening: Runtime state recovery preserves the corrupt database and sidecars, updater integrity-checks SQLite before switching releases, and rollback/migration moves the database together with its WAL/SHM artifacts.
- 2026-08-08 Coding action contract repair: Execution Planning canonicalizes a Hermes `coding.turn.start` `instruction` payload to the registered Edge `task` schema before injecting the Runtime-owned workspace and Interaction identifiers, preventing a valid coding request from being rejected as an unregistered capability.
- 2026-08-24 M18 Runtime acceptance: parallel per-Interaction workers, non-blocking Gateway ingress, a persistent Main Hermes semantic session, Child Session context relief, and versioned Runtime state/evidence projection are accepted as one coupled Runtime/Hermes loop. The remaining real Coding workflow/concurrent-child scenario is a separate cross-cutting acceptance under [#13](https://github.com/nwomn/openhalo/issues/13).
- 2026-08-09 non-blocking Gateway ingress acceptance slice completed: authenticated WebSocket frames now start independently, while frames carrying the same explicit Interaction identity (including Coding activity observations) are serialized in arrival order. Capability registration remains a short ordered ingress prerequisite and outbound frames remain serialized per WebSocket. This removes the observed background-coding-to-normal-dialogue head-of-line block without changing the Gateway -> Agent Runtime architecture; later M18 completed the generic per-Interaction worker, priority, coalescing, and Main Hermes Runtime loop.
- 2026-08-09 manual acceptance evidence: while Coding `interaction-62` was active, status-query `interaction-63` completed at 08:30:18 UTC before the Coding `turn_completed` observation at 08:30:53 UTC; a later `interaction-64` correctly reported the completed state. This accepts removal of the foreground-dialogue head-of-line block. The same run also exposed a result-projection gap, subsequently addressed by the accepted bounded ContextFact/ContextEnvelope projection. The full real Coding workflow/concurrent-child scenario is independently tracked by [#13](https://github.com/nwomn/openhalo/issues/13).
- 2026-08-09 Main Hermes architecture clarification: the persistent Main Session spans semantic Proposal Formation and semantic Execution Planning across the Agent Runtime, so one continuous memory can connect user intent, process understanding, proposal, action intent, and result interpretation. Runtime retains deterministic action validation/execution boundaries, while Presence Router remains an explicit model-independent governance gate; Child Sessions handle process-local context and return bounded semantic deltas to Main.
- 2026-08-23 registered-Edge context decision: registering a schema-valid Observation provider is the default context-admission path for OpenHalo personality. Runtime generically projects every accepted registered Observation into a provenance-bearing, freshness-aware context fact for Main Hermes, without per-device or per-Observation Runtime reducer/prompt code. Runtime's shared schema, bounded-value, coalescing, stale/`unknown`, and raw-media/evidence boundaries remain; Presence Router separately governs proactive action. The former fixed compact snapshot remains only as a temporary Presence compatibility projection, not personality context.
- 2026-08-23 M18 refactor contract documented: replace `fixed observation name -> fixed compact snapshot field` with `registration registry -> generic ContextFact table -> versioned ContextEnvelope -> Main Hermes`. The migration retains a temporary legacy projection only for already-shipped Presence rules and has explicit unknown/stale, same-name multi-device, bounded-query, and real-MaixCAM-to-phone acceptance criteria.
- 2026-08-23 M18 implementation layout: the architecture diagram now renders `Gateway -> ContextFact Materializer/SQLite -> ContextEnvelope Compiler -> InteractionScheduler -> Main Hermes or per-Interaction Child -> Presence -> Runtime Validation -> Action Layer`; evidence escalation is a correlated `context.evidence.read` Edge action. This makes the implemented internal modules and their one-way boundaries explicit without turning Gateway or the Orchestrator into a multi-layer service.
- The accepted target layout, concurrency rules, authority boundaries, and Main/Child context contract are documented in [Main Hermes And Parallel Interaction Runtime Design](docs/design/2026-08-09-main-hermes-interaction-runtime-design.md).
- Cross-boundary traffic must continue through `Edge Session Link <-> Gateway`.
- Presence remains an explicit, inspectable governance layer inside `Agent Runtime`.
- 2026-08-03 Coding Agent Bridge direction: the Codex-first bridge is a cross-cutting capability inside the existing Terminal Edge, not a new Device Edge identity or coding-specific Runtime lifecycle. Terminal Edge owns local App Server observation, bounded evidence, and confirmed turn steering; the accepted M18 Runtime supplies generic attention admission, `experience_discovery`, Interaction Pool registration, Presence, and proposal governance; M20.1 remains the later owner of governed Skill drafts. The delivery is tracked under [#10](https://github.com/nwomn/openhalo/issues/10) with design child [#11](https://github.com/nwomn/openhalo/issues/11); its real workflow/concurrent-child acceptance remains [#13](https://github.com/nwomn/openhalo/issues/13) and does not alter the active `M17.8 -> M17.11 -> M17.9 -> M17.10` route.
- 2026-08-03 hosted Coding Bridge implementation: Terminal Edge now owns a managed stdio Codex App Server lifecycle, one independent Codex thread per OpenHalo interaction, bounded `coding.attention.v1` evidence, `coding.turn.start`, confirmed `coding.turn.steer`, and local Codex approval handling. Runtime remains the authority for task interpretation and governance; no second Device Edge identity or provider-specific Runtime shortcut was introduced.
- 2026-08-04 Issue [#16](https://github.com/nwomn/openhalo/issues/16) implementation direction: the existing hosted bridge gains an expandable structured Coding activity panel and exact foreground correction/interrupt routing through the outer composer. New activity events use the neutral `coding.activity.v1` ordinary-observation vocabulary; the previous `coding.attention.v1` name remains readable only for historical compatibility and is not dual-written. Edge-local active-task journals are paged and durable without a small per-task event ceiling; the default concurrent-active guard is 32 and completed-task cleanup is capacity-based. This cross-cutting work does not alter the active `M17.8 -> M17.11 -> M17.9 -> M17.10` route.
- 2026-08-04 Issue [#16](https://github.com/nwomn/openhalo/issues/16) Edge acceptance: a real paired Terminal Edge displayed live Coding activity, selected an explicit task, routed outer-composer correction to the exact Codex turn, interrupted it with `Esc`, and loaded local history with `PageUp`. Runtime received `coding.activity.v1` as an ordinary observation with reasoning, command, test, agent-message, correction, and turn-lifecycle events. The acceptance also found and fixed late same-turn activity reopening a completed local task; the focused Issue #16 regression set passes 88 tests.
- 2026-08-05 Issue [#16](https://github.com/nwomn/openhalo/issues/16) activity-stream follow fix: a second acceptance pass found the Coding activity panel appeared stuck at the first minutes while the task kept running. Runtime and Edge-local records proved the data path was healthy (runtime received `coding.activity` until 08:38:20Z; the Edge journal held 46 events and the task completed), and the root cause was purely presentational: `#coding-activity-log` was a fixed-height `Static`, whose virtual size never grows with content, so overflow was clipped and no scrolling existed. The fix wraps the text in a `VerticalScroll` container (`#coding-activity-scroll`) whose virtual size tracks content, adds anchored bottom-follow on each refresh, pauses follow while the user scrolls up and resumes at the bottom, and keeps follow paused during `PageUp` history paging. Two auto-scroll regression tests were added; the full regression passes 858 tests (4 intentional skips, 19 subtests). Published as `v0.1.15` with immutable GitHub Release assets (`openhalo-v0.1.15.tar.gz`, `release-manifest.json`, `SHA256SUMS`).
- 2026-08-09 Issue [#16](https://github.com/nwomn/openhalo/issues/16) is formally closed: its Terminal Edge activity-window scope is accepted and complete. It remains a completed Coding Bridge work item, not a separate Runtime milestone; its standalone evidence did not determine the later M18 acceptance.
- 2026-08-04 Issue [#14](https://github.com/nwomn/openhalo/issues/14) acceptance: focused bridge/runtime coverage and the full Python regression passed (730 tests, 4 intentional skips); owner Terminal Edge acceptance kept the existing P-256 device/session boundary, completed local Codex approval, created and ran `openhalo_codex_demo.py`, and observed the Codex turn completion. Issue #14 is accepted for its adapter scope; the separate Runtime receipt-timing follow-up remains outside this closeout.
- Detailed architecture and milestone evidence are loaded from linked documents rather than repeated in this baseline.

### Current acceptance anchors

- `M18.1` is accepted; mobile observation evidence now enters the compact snapshot decision space with freshness-aware handling.
- `M18` is accepted; generic registered facts now materialize through ContextFact and ContextEnvelope into the persistent Main Hermes session. The owner live acceptance covers native session recovery and MaixCAM `present`/`absent` semantic facts queried correctly on the phone, with no raw-media fields in Runtime persistence. Real Coding workflow/concurrent-child acceptance is explicitly deferred to [#13](https://github.com/nwomn/openhalo/issues/13).
- `M19` is accepted; SQLite-backed bounded persistence, retention, diagnostics rotation, migration, and owner-runtime acceptance are documented in the [M19 operational archive](/root/openhalo/docs/history/m19-operational-status.md).
- `M20`, `M20.2`, and `M20.3` are accepted; their detailed Harness, progress, Terminal, Android, and human-acceptance evidence remains in the [completed progress archive](/root/openhalo/docs/history/project-completed-progress.md).
- Hosted Coding Bridge adapter ([#14](https://github.com/nwomn/openhalo/issues/14)) is accepted; the separate Runtime receipt-timing follow-up remains outside this closeout.
- `M22` productization remains an active Goal 5 direction; packaging, update, pairing, and three-end deployment details must be checked against the latest implementation documents and Issues.

### Next execution route

1. `M17.11` Proxy Interaction Edge baseline.
2. `M17.10` Ambient Home Presence Edge baseline.
3. `M20.1` governed procedural-memory and skill lifecycle.
4. `M21` policy learning and review.
5. `M22` packaged three-end product slice.
6. `M23` Home Assistant Bridge Edge and smart-home ecosystem bridge.
7. `M24` Cellular Camera Edge product hardware baseline.

`M17.8` mobile sensitive-screen capture governance and `M17.9` native Windows
Desktop Edge remain planned `Backlog` work for the current execution window and
therefore do not gate this route.

For detailed evidence, read the linked archives or the relevant milestone plan/Issue rather than expanding this section with another historical log.

## Progress Update Rules

When updating this file:

- Keep `Current Snapshot`, goal statuses, the active route, and acceptance summaries current.
- Keep completed work clearly marked in the `Completed Progress Index`; preserve detailed evidence in the linked archive.
- Add new milestones or sub-goals only when they become concrete enough to evaluate.
- Do not mark a sub-goal complete unless its acceptance criteria are satisfied.
- Do not copy detailed implementation history, raw investigation logs, or low-frequency research questions back into this baseline.
